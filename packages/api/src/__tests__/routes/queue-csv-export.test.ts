// Wave 0 stub -- implementation lands in Plan 03. Replace it.todo with real assertions during the GREEN phase.
import { describe, it } from 'vitest';

describe('queue posts CSV export route', () => {
  it.todo('streams queue posts via Transfer-Encoding chunked');
  it.todo('sets Content-Disposition with date-stamped queue-posts filename');
  it.todo('scopes exported queue posts to the authenticated user');
  it.todo('requires an authenticated session');
});
