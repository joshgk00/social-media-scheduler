// Wave 0 stub -- implementation lands in Plan 03. Replace it.todo with real assertions during the GREEN phase.
import { describe, it } from 'vitest';

describe('posts CSV export route', () => {
  it.todo('streams via Transfer-Encoding chunked');
  it.todo('sets Content-Disposition with date-stamped posts filename');
  it.todo('respects postQuerySchema filters for status, profileId, tag, and q');
  it.todo('requires an authenticated session');
});
