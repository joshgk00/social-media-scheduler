// Wave 0 stub -- implementation lands in Plan 03. Replace it.todo with real assertions during the GREEN phase.
import { describe, it } from 'vitest';

describe('bulk operation idempotency', () => {
  it.todo('double-submit returns the same bulk_operations row');
  it.todo('stores idempotencyKey UUID on enqueue');
});
