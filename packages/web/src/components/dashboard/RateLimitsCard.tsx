/**
 * Stub for Plan 05b. The real implementation ships in
 * `08-05b-dashboard-and-rate-limit-chip`. This stub exists only so
 * `tsc -b` succeeds in Plan 05a — the component is referenced by a
 * Plan 01 RED test (`__tests__/RateLimitsCard.test.tsx`) that Plan 05b
 * will turn GREEN.
 */
export function RateLimitsCard() {
  return null;
}
