---
phase: 06-media-handling
plan: 04
type: execute
wave: 2
depends_on:
  - "06-02"
files_modified:
  - packages/web/src/components/posts/MediaDropZone.tsx
  - packages/web/src/components/posts/MediaThumbnailGrid.tsx
  - packages/web/src/components/posts/MediaThumbnail.tsx
  - packages/web/src/hooks/use-media-upload.ts
  - packages/web/src/hooks/use-media.ts
  - packages/web/src/pages/posts/NewPostPage.tsx
  - packages/web/src/pages/posts/EditPostPage.tsx
  - packages/web/src/pages/posts/PostsPage.tsx
  - packages/web/src/lib/api-client.ts
  - packages/web/src/components/ui/progress.tsx
autonomous: false
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04

must_haves:
  truths:
    - "User can drag-and-drop or click to upload media files in the post creation form"
    - "Uploaded images display as thumbnails in a sortable grid with drag-to-reorder"
    - "Upload progress is shown as a percentage overlay on each thumbnail"
    - "Video thumbnails show transcoding status (queued, transcoding, complete, failed)"
    - "Schedule/Publish button is disabled while any media is transcoding"
    - "Client-side validation rejects files that exceed platform limits before upload"
    - "User can remove attached media via X button on each thumbnail"
    - "Post list rows show media indicator icon with count"
    - "Edit post page loads existing media and supports add/remove/reorder"
  artifacts:
    - path: "packages/web/src/components/posts/MediaDropZone.tsx"
      provides: "Drag-and-drop upload zone component"
    - path: "packages/web/src/components/posts/MediaThumbnailGrid.tsx"
      provides: "Sortable thumbnail grid using @dnd-kit"
    - path: "packages/web/src/components/posts/MediaThumbnail.tsx"
      provides: "Individual thumbnail with status overlays"
    - path: "packages/web/src/hooks/use-media-upload.ts"
      provides: "XHR-based upload with progress tracking"
    - path: "packages/web/src/hooks/use-media.ts"
      provides: "TanStack Query hooks for media status polling"
  key_links:
    - from: "packages/web/src/pages/posts/NewPostPage.tsx"
      to: "packages/web/src/components/posts/MediaDropZone.tsx"
      via: "post form renders MediaDropZone below text area"
      pattern: "MediaDropZone"
    - from: "packages/web/src/components/posts/MediaDropZone.tsx"
      to: "packages/web/src/hooks/use-media-upload.ts"
      via: "drop zone triggers upload hook on file drop"
      pattern: "useMediaUpload"
    - from: "packages/web/src/components/posts/MediaThumbnailGrid.tsx"
      to: "packages/web/src/hooks/use-media.ts"
      via: "grid polls transcoding status via TanStack Query"
      pattern: "useMediaStatus"
---

<objective>
Build the frontend media upload experience: drag-and-drop zone, thumbnail grid with sortable reorder, upload progress tracking, transcoding status indicators, and client-side validation. Replace the Phase 3 placeholder in NewPostPage.tsx with fully functional media attachment. Extend EditPostPage with media editing. Add media indicators to post list rows.

Purpose: This is the user-facing media experience -- the interface between the user and the upload/transcode pipeline built in Plans 02/03.
Output: MediaDropZone, MediaThumbnailGrid, MediaThumbnail components, useMediaUpload and useMedia hooks, updated NewPostPage/EditPostPage/PostsPage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-media-handling/06-CONTEXT.md
@.planning/phases/06-media-handling/06-RESEARCH.md
@.planning/phases/06-media-handling/06-UI-SPEC.md
@.planning/phases/06-media-handling/06-02-SUMMARY.md

@packages/web/src/pages/posts/NewPostPage.tsx
@packages/web/src/pages/posts/EditPostPage.tsx
@packages/web/src/pages/posts/PostsPage.tsx
@packages/web/src/hooks/use-posts.ts
@packages/web/src/lib/api-client.ts
@packages/web/src/components/posts/ThreadEditor.tsx
@packages/web/CLAUDE.md
@packages/web/components.json
@packages/shared/src/constants/media-limits.ts

<interfaces>
<!-- API endpoints from Plan 02 -->

POST /api/media/upload (multipart/form-data)
  Body: file (binary), profileId (string), platform (string)
  Response 201: { id, fileName, mimeType, fileSize, thumbnailUrl, transcodeStatus }

GET /api/media/:id/status
  Response 200: { id, transcodeStatus, transcodeError }

DELETE /api/media/:id
  Response 204

POST /api/posts (existing, updated)
  Body: { ...existingFields, mediaIds?: string[] }

PUT /api/posts/:id (existing, updated)
  Body: { ...existingFields, mediaIds?: string[] }

<!-- From packages/shared/src/constants/media-limits.ts -->
PLATFORM_MEDIA_LIMITS: Record<string, PlatformMediaLimits>

<!-- From packages/web/src/lib/api-client.ts -->
apiClient.postFormData<T>(path: string, formData: FormData): Promise<T>
getCsrfToken(): Promise<string>  (module-level, exported for XHR usage)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn progress, create hooks and media components</name>
  <files>
    packages/web/src/components/ui/progress.tsx
    packages/web/src/hooks/use-media-upload.ts
    packages/web/src/hooks/use-media.ts
    packages/web/src/components/posts/MediaDropZone.tsx
    packages/web/src/components/posts/MediaThumbnailGrid.tsx
    packages/web/src/components/posts/MediaThumbnail.tsx
    packages/web/src/lib/api-client.ts
  </files>
  <read_first>
    packages/web/src/pages/posts/NewPostPage.tsx
    packages/web/src/lib/api-client.ts
    packages/web/src/hooks/use-posts.ts
    packages/web/src/components/posts/ThreadEditor.tsx
    packages/web/CLAUDE.md
    packages/web/components.json
    packages/web/src/index.css
    packages/shared/src/constants/media-limits.ts
    .planning/phases/06-media-handling/06-UI-SPEC.md
  </read_first>
  <action>
    **0. Install shadcn progress component:**
    ```bash
    cd packages/web && npx shadcn@latest add progress --yes
    ```

    **1. Export getCsrfToken from api-client.ts:**
    The XHR upload function needs the CSRF token. In `packages/web/src/lib/api-client.ts`, export `getCsrfToken` so the upload hook can use it. The function already exists as module-level -- just add `export` keyword to `async function getCsrfToken()`.

    **2. Upload hook** (`packages/web/src/hooks/use-media-upload.ts`):
    Export `useMediaUpload()` hook that returns `{ uploadFile, isUploading }`:
    - `uploadFile(file: File, profileId: string, platform: string)` returns `Promise<MediaUploadResponse>`
    - Uses `XMLHttpRequest` for upload progress tracking (per RESEARCH.md discretion -- XHR is reliable for upload.onprogress)
    - Track per-file progress state: `Record<string, number>` keyed by temp file ID
    - Implementation:
      ```typescript
      export function uploadMediaFile(
        file: File,
        profileId: string,
        platform: string,
        csrfToken: string,
        onProgress: (percent: number) => void,
      ): Promise<MediaUploadResponse> {
        return new Promise((resolve, reject) => {
          const formData = new FormData();
          formData.append('file', file);
          formData.append('profileId', profileId);
          formData.append('platform', platform);
          const xhr = new XMLHttpRequest();
          xhr.open('POST', '/api/media/upload');
          xhr.setRequestHeader('x-csrf-token', csrfToken);
          xhr.withCredentials = true;
          xhr.upload.onprogress = (event) => {
            if (event.lengthComputable) {
              onProgress(Math.round((event.loaded / event.total) * 100));
            }
          };
          xhr.onload = () => {
            if (xhr.status >= 200 && xhr.status < 300) {
              resolve(JSON.parse(xhr.responseText));
            } else {
              try { reject(new Error(JSON.parse(xhr.responseText)?.error || 'Upload failed')); }
              catch { reject(new Error('Upload failed')); }
            }
          };
          xhr.onerror = () => reject(new Error('Network error during upload'));
          xhr.send(formData);
        });
      }
      ```
    - The `useMediaUpload` hook wraps this: manages a `Map<string, { progress: number, status: 'uploading' | 'done' | 'error' }>`, provides `upload(file, profileId, platform): Promise<MediaUploadResponse>` that creates a temp ID, tracks progress, returns the server response.

    **3. Media query hooks** (`packages/web/src/hooks/use-media.ts`):

    `useMediaStatus(mediaId: string, enabled: boolean)`:
    - TanStack Query: `queryKey: ['media-status', mediaId]`
    - `queryFn`: `apiClient.get('/api/media/' + mediaId + '/status')`
    - `refetchInterval: 3000` (3-second polling per UI-SPEC)
    - `enabled`: only when `transcodeStatus === 'pending' || 'processing'` AND `!document.hidden`
    - Stop polling when status reaches 'completed', 'failed', or 'not_applicable'

    `useDeleteMedia()`:
    - TanStack Query mutation: `mutationFn(mediaId: string)`: `apiClient.delete('/api/media/' + mediaId)`

    `useStorageUsage()`:
    - TanStack Query: `queryKey: ['storage-usage']`
    - `queryFn`: `apiClient.get('/api/settings/storage')`
    - Used by StorageUsageCard in Plan 05

    **4. MediaDropZone** (`packages/web/src/components/posts/MediaDropZone.tsx`):
    Props: `{ platform: string | null, existingMediaCount: number, maxFiles: number, onFilesSelected: (files: File[]) => void, disabled?: boolean }`

    Per UI-SPEC "Media Drop Zone" interaction states:
    - **Idle (no files):** Dashed border (`border-2 border-dashed border-border rounded-lg bg-card/50`), centered `Upload` icon (lucide) at 32px + two lines: "Drop files or click to upload" (`text-sm`) and "Images, GIFs, or video" (`text-xs text-muted-foreground`). `p-8` padding. Clickable via hidden `<input type="file">`.
    - **Idle (has files, under limit):** Compact strip: `p-4`, `Upload` icon at 16px + "Add more files" text. Still clickable and droppable.
    - **Idle (at file limit):** Drop zone hidden entirely (return null).
    - **Drag-over:** Border `border-primary border-solid`, background `bg-primary/5`, text "Drop to upload".
    - **Drag-over invalid:** Border `border-destructive border-solid`, background `bg-destructive/5`, text "Unsupported file type".
    - **Disabled (publishing state):** Return null (hidden).

    Client-side validation on file selection (per D-03):
    - Look up `PLATFORM_MEDIA_LIMITS[platform]` for limits
    - Check each file: type in allowedImageTypes or allowedVideoTypes, size under maxImageSizeMb or maxVideoSizeMb
    - Check count: existingMediaCount + newFiles.length <= maxImages (images) or maxVideos (videos)
    - Check mixed media: cannot have both images and videos in the same post
    - Rejected files: show toast via sonner with the specific error message from UI-SPEC copywriting contract
    - Accepted files: call `onFilesSelected(validFiles)`

    Platform limit hint text below drop zone (per UI-SPEC):
    - Twitter: "Twitter: up to 4 images (5 MB each) or 1 video (15 MB)"
    - LinkedIn: "LinkedIn: 1 image (20 MB) or 1 video (200 MB)"
    - Facebook: "Facebook: up to 10 images (5 MB each) or 1 video (100 MB)"
    - No profile selected: "Select a profile to see media limits."

    `accept` attribute on file input: derive from platform's `allowedImageTypes` + `allowedVideoTypes`, comma-separated.

    Accessibility (per UI-SPEC):
    - Hidden input is focusable
    - Drop zone wrapper: `role="button"`, `tabIndex={0}`
    - Press Enter or Space opens file picker
    - `aria-label="Upload media files"`
    - Drag-over announced via `aria-live="polite"` region

    **5. MediaThumbnail** (`packages/web/src/components/posts/MediaThumbnail.tsx`):
    Props: `{ media: MediaItem, uploadProgress?: number, isUploading: boolean, onRemove: () => void, showDragHandle: boolean, readOnly: boolean, dragAttributes?: any, dragListeners?: any }`

    `MediaItem` type: `{ id: string, fileName: string, mimeType: string, thumbnailUrl: string | null, transcodeStatus: TranscodeStatus, transcodeError: string | null }`

    Per UI-SPEC thumbnail states:
    - Container: `aspect-square rounded-md overflow-hidden relative bg-card`
    - Image: `object-cover w-full h-full` using thumbnailUrl
    - Video (no thumbnail): centered `Film` icon (lucide, 32px, `text-muted-foreground`) with fileName below
    - **Uploading:** Semi-transparent overlay (`bg-background/60`). `Progress` bar centered. `{percent}%` text (`text-xs`) below bar. No remove button. No drag handle.
    - **Upload complete (image):** Overlay fades. Remove button and drag handle visible.
    - **Upload complete (video, transcoding):** Spinner (`Loader2` 24px animate-spin) + "Transcoding..." (`text-xs text-warning`). Remove button visible. No drag handle.
    - **Transcode queued:** `Loader2` 24px (static, no spin) + "Queued..." (`text-xs text-muted-foreground`)
    - **Transcode complete:** Green check (`CheckCircle` 24px `text-success`) for 1.5s, then fades.
    - **Transcode failed:** Red overlay `bg-destructive/10`. `AlertCircle` 24px `text-destructive`. Error text `text-xs text-destructive`. "Retry" link (`text-primary cursor-pointer`) and "Remove" link (`text-destructive cursor-pointer`).
    - **Drag active:** `opacity-50 ring-2 ring-primary`
    - **Read-only:** No remove button, no drag handle, `opacity-80`

    Remove button: absolute top-right, `p-1` circle, `bg-background/80 backdrop-blur-sm`, `X` icon 14px, hover `text-destructive`. `aria-label="Remove {fileName}"`.
    Drag handle: absolute top-left, `GripVertical` 14px, `text-muted-foreground`, `cursor-grab`. Only shown when `showDragHandle && !readOnly && !isUploading`. `aria-label="Reorder {fileName}"`.

    **6. MediaThumbnailGrid** (`packages/web/src/components/posts/MediaThumbnailGrid.tsx`):
    Props: `{ mediaItems: MediaItem[], uploadingFiles: Map<string, { progress: number }>, onRemove: (id: string) => void, onReorder: (ids: string[]) => void, onRetryTranscode: (id: string) => void, readOnly: boolean }`

    Uses `@dnd-kit/sortable` with `rectSortingStrategy` (per RESEARCH.md -- same pattern as ThreadEditor.tsx but grid strategy instead of vertical):
    - `DndContext` with sensors (pointer + keyboard)
    - `SortableContext` with `rectSortingStrategy` wrapping media items
    - Each item wrapped in `useSortable` providing `attributes`, `listeners`, `setNodeRef`, `transform`, `transition`
    - Grid layout: `grid grid-cols-2 gap-3 md:grid-cols-4`
    - `handleDragEnd`: reorder the mediaItems array and call `onReorder(newOrder)`

    Poll transcoding status: use `useMediaStatus` for each media item with `transcodeStatus === 'pending' || 'processing'`. When status changes, update the local state.

    **7. Wire into NewPostPage.tsx:**
    Replace the placeholder div (lines 321-327) with:
    ```tsx
    <MediaDropZone
      platform={watchedProfilePlatform}
      existingMediaCount={mediaItems.length}
      maxFiles={maxFilesForPlatform}
      onFilesSelected={handleFilesSelected}
      disabled={isSubmitting}
    />
    {mediaItems.length > 0 && (
      <MediaThumbnailGrid
        mediaItems={mediaItems}
        uploadingFiles={uploadingFiles}
        onRemove={handleRemoveMedia}
        onReorder={handleReorderMedia}
        onRetryTranscode={handleRetryTranscode}
        readOnly={false}
      />
    )}
    ```

    Add state management:
    - `const [mediaItems, setMediaItems] = useState<MediaItem[]>([])` -- tracks attached media
    - `const [uploadingFiles, setUploadingFiles] = useState(new Map())` -- tracks upload progress
    - `handleFilesSelected`: for each file, call `uploadMediaFile()` with progress callback, on success add to `mediaItems`
    - `handleRemoveMedia`: call `deleteMedia` mutation, remove from `mediaItems`
    - `handleReorderMedia`: update `mediaItems` order
    - `handleRetryTranscode`: POST /api/media/:id/retry (or re-enqueue -- if not implemented, just log for now)

    Update submit: include `mediaIds: mediaItems.map(m => m.id)` in the post save request body.

    Disable submit button when any media has `transcodeStatus === 'pending' || 'processing'` or `'failed'`:
    - Add tooltip per UI-SPEC: "Video is still transcoding." or "Fix or remove failed media before submitting."
    - Draft save remains available regardless of transcode status (per D-07).

    **8. Wire into EditPostPage.tsx:**
    - On mount, fetch existing media for the post and populate `mediaItems`
    - Same drop zone + grid integration as NewPostPage
    - Existing media displayed with thumbnails from server
    - User can remove (soft-delete), add new, reorder
    - If post is in `publishing` state: `readOnly={true}`, no drop zone (STATE-02)

    **9. Post list media indicator (PostsPage.tsx):**
    Per UI-SPEC "Media Indicator in Post Lists":
    - In the post text column, when post has media: add `Image` icon (lucide, 14px) + `{count}` in `text-xs text-muted-foreground`
    - Tooltip: "{count} image(s)" or "1 video" or "{count} image(s), 1 video (transcoding)"
    - If any media transcoding: show `Loader2` (12px animate-spin) instead of `Image` icon with `aria-label="Media transcoding in progress"`
    - This requires the post list API to include media count/status -- if not already returned, add `mediaCount` and `hasTranscodingMedia` fields to the post list response (in the posts route)
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && pnpm --filter @sms/web build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/components/ui/progress.tsx exists (shadcn progress component)
    - packages/web/src/components/posts/MediaDropZone.tsx exists and contains `role="button"`, `tabIndex={0}`, `aria-label="Upload media files"`
    - packages/web/src/components/posts/MediaDropZone.tsx imports and uses `PLATFORM_MEDIA_LIMITS`
    - packages/web/src/components/posts/MediaThumbnailGrid.tsx exists and imports from `@dnd-kit/sortable`
    - packages/web/src/components/posts/MediaThumbnailGrid.tsx uses `rectSortingStrategy`
    - packages/web/src/components/posts/MediaThumbnail.tsx exists with states for uploading, transcoding, failed, completed
    - packages/web/src/hooks/use-media-upload.ts exports `uploadMediaFile` function using `XMLHttpRequest`
    - packages/web/src/hooks/use-media.ts exports `useMediaStatus` with `refetchInterval: 3000`
    - packages/web/src/pages/posts/NewPostPage.tsx imports `MediaDropZone` and `MediaThumbnailGrid`
    - packages/web/src/pages/posts/NewPostPage.tsx does NOT contain the old placeholder comment "Phase 6 implements actual media upload"
    - packages/web/src/pages/posts/PostsPage.tsx contains media indicator with `Image` icon from lucide
    - `pnpm --filter @sms/web build` succeeds with 0 errors
  </acceptance_criteria>
  <done>Complete frontend media experience: drag-and-drop upload with progress, sortable thumbnail grid, transcoding status indicators, client-side validation per platform, edit post media support, post list media indicators</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify media upload UI end-to-end</name>
  <files>packages/web/src/pages/posts/NewPostPage.tsx</files>
  <action>
    Human verifies the full media upload and preview system integrated into the post creation form. This covers: drag-and-drop upload zone, thumbnail grid with sortable reorder, upload progress bars, transcoding status indicators (spinner for processing, red overlay for failed), client-side platform-specific validation, edit post media support, and post list media indicators.

    Steps to verify:
    1. Start dev environment: `docker compose -f docker-compose.yml -f docker-compose.dev.yml up`
    2. Navigate to the post creation page
    3. Verify the drop zone appears below the text area with "Drop files or click to upload" text
    4. Select a Twitter profile -- verify the hint text shows "Twitter: up to 4 images (5 MB each) or 1 video (15 MB)"
    5. Upload an image -- verify progress bar shows during upload, thumbnail appears in the grid after upload, X button appears to remove, drag handle appears for reordering
    6. Upload 2+ images -- verify you can drag to reorder
    7. Try uploading an oversized file -- verify toast error with size limit message
    8. Try uploading an unsupported file type -- verify toast error
    9. Upload a video -- verify "Transcoding..." spinner overlay appears, Schedule button is disabled with tooltip "Video is still transcoding.", status updates to completed or shows error
    10. Save a post with media as draft -- verify media is preserved
    11. Navigate to the posts list -- verify media indicator (image icon + count) appears
    12. Edit a post with existing media -- verify thumbnails are pre-populated
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && pnpm --filter @sms/web build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms: drop zone renders correctly, image upload with progress works, thumbnail grid with reorder works, video transcoding status displays correctly, client validation catches invalid files, edit post loads existing media, post list shows media indicators</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side validation | First-pass filter only; server validates authoritatively |
| CSRF on upload | XHR includes x-csrf-token header from api-client getCsrfToken() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-13 | Spoofing | Upload without authentication | mitigate | XHR includes withCredentials:true (session cookie) + x-csrf-token header; server requireAuth middleware validates both |
| T-06-14 | Tampering | Client-side validation bypass | accept | Client validation is UX convenience only; server validates authoritatively in media route handler (T-06-04, T-06-05) |
| T-06-15 | Information Disclosure | Thumbnail URLs | accept | Thumbnails served from /media/ path; single-user app with no public access. UUIDs in paths provide unpredictability. |
</threat_model>

<verification>
- `pnpm --filter @sms/web build` succeeds
- Visual inspection of drop zone, thumbnail grid, transcoding states
- Upload flow works end-to-end (requires running API from Plan 02)
</verification>

<success_criteria>
User can attach media to posts via drag-and-drop, see upload progress, reorder images, monitor video transcoding, and submit posts with media IDs. The UI correctly enforces platform-specific limits client-side and disables submission during transcoding.
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-handling/06-04-SUMMARY.md`
</output>
