---
phase: 06-media-handling
plan: 06
type: execute
wave: 5
depends_on:
  - "06-01"
  - "06-05"
files_modified:
  - packages/db/drizzle/0003_phase-06-media-handling.sql
  - packages/db/drizzle/meta/_journal.json
  - packages/db/drizzle/meta/0003_snapshot.json
autonomous: true
gap_closure: true
requirements:
  - MEDIA-01
  - MEDIA-02
  - MEDIA-03
  - MEDIA-04
  - MEDIA-05
  - MEDIA-06
  - MEDIA-07
  - MEDIA-08
  - MEDIA-09

must_haves:
  truths:
    - "A migration SQL file exists in packages/db/drizzle/ that creates the transcode_status enum and adds transcode_status, transcode_error, deleted_at columns to post_media"
    - "The migration makes post_id nullable (ALTER COLUMN post_id DROP NOT NULL)"
    - "The migration adds indexes on deleted_at and transcode_status columns"
    - "The drizzle meta journal includes an entry for the Phase 6 migration"
  artifacts:
    - path: "packages/db/drizzle/0003_phase-06-media-handling.sql"
      provides: "Phase 6 schema migration SQL"
      contains: "transcode_status"
    - path: "packages/db/drizzle/meta/_journal.json"
      provides: "Updated migration journal with Phase 6 entry"
      contains: "phase-06-media-handling"
  key_links:
    - from: "packages/db/drizzle/0003_phase-06-media-handling.sql"
      to: "packages/db/src/schema/post-media.ts"
      via: "drizzle-kit generate reads schema and produces migration"
      pattern: "transcode_status"
---

<objective>
Generate the drizzle-kit migration SQL for Phase 6 schema changes (transcode_status enum, transcode_status/transcode_error/deleted_at columns, nullable postId, new indexes on post_media table).

Purpose: Close the single BLOCKING gap from 06-VERIFICATION.md. Without this migration, the database lacks the columns Phase 6 code depends on -- every media upload, transcode status check, soft-delete, and cleanup query would fail at runtime.

Output: Migration SQL file at packages/db/drizzle/0003_phase-06-media-handling.sql, updated journal and snapshot in drizzle/meta/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-media-handling/06-VERIFICATION.md
@.planning/phases/06-media-handling/06-05-SUMMARY.md
@packages/db/src/schema/post-media.ts
@packages/db/drizzle.config.ts
@packages/db/drizzle/meta/_journal.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate drizzle-kit migration for Phase 6 post_media schema changes</name>
  <files>packages/db/drizzle/0003_phase-06-media-handling.sql, packages/db/drizzle/meta/_journal.json, packages/db/drizzle/meta/0003_snapshot.json</files>
  <read_first>
    - packages/db/src/schema/post-media.ts (the schema source of truth -- already correct, has transcodeStatusEnum, transcodeStatus, transcodeError, deletedAt, nullable postId)
    - packages/db/drizzle.config.ts (drizzle-kit configuration -- schema path, output path, dialect)
    - packages/db/drizzle/meta/_journal.json (current journal with 3 entries: 0000, 0001, 0002)
    - packages/db/drizzle/0002_phase-04-publish-worker.sql (most recent migration -- confirms current DB state)
    - packages/db/src/schema/index.ts (all schema exports -- drizzle-kit reads this via config)
  </read_first>
  <action>
    Run drizzle-kit generate from the packages/db directory to produce the migration SQL for Phase 6 schema changes.

    Command:
    ```
    cd packages/db && npx drizzle-kit generate --name phase-06-media-handling
    ```

    This will:
    1. Read the schema from `./src/schema/index.ts` (per drizzle.config.ts)
    2. Compare against the last snapshot (0002_snapshot.json)
    3. Generate migration SQL file at `./drizzle/0003_phase-06-media-handling.sql`
    4. Create `./drizzle/meta/0003_snapshot.json`
    5. Update `./drizzle/meta/_journal.json` with the new entry

    The generated SQL should contain:
    - CREATE TYPE "transcode_status" AS ENUM ('pending', 'processing', 'completed', 'failed', 'not_applicable')
    - ALTER TABLE "post_media" ADD COLUMN "transcode_status" "transcode_status" DEFAULT 'not_applicable' NOT NULL
    - ALTER TABLE "post_media" ADD COLUMN "transcode_error" text
    - ALTER TABLE "post_media" ADD COLUMN "deleted_at" timestamp with time zone
    - ALTER TABLE "post_media" ALTER COLUMN "post_id" DROP NOT NULL
    - CREATE INDEX "post_media_deleted_at" ON "post_media" USING btree ("deleted_at")
    - CREATE INDEX "post_media_transcode_status" ON "post_media" USING btree ("transcode_status")

    After generation, verify the output by reading the generated SQL file to confirm all expected DDL statements are present. If drizzle-kit requires DATABASE_URL to be set for generation, use a dummy value -- generate does not connect to a database, it only compares schema snapshots.

    NOTE: Do NOT run `drizzle-kit migrate` or `drizzle-kit push`. The migration SQL file generation is the deliverable. Applying it to a running database is a runtime concern handled by container startup or manual dev setup.
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler/packages/db && test -f drizzle/0003_phase-06-media-handling.sql && grep -q "transcode_status" drizzle/0003_phase-06-media-handling.sql && grep -q "transcode_error" drizzle/0003_phase-06-media-handling.sql && grep -q "deleted_at" drizzle/0003_phase-06-media-handling.sql && grep -q "phase-06-media-handling" drizzle/meta/_journal.json && echo "PASS: Migration file generated with all expected columns and journal updated" || echo "FAIL: Missing migration file or expected content"</automated>
  </verify>
  <acceptance_criteria>
    - File `packages/db/drizzle/0003_phase-06-media-handling.sql` exists
    - Migration SQL contains `transcode_status` (the enum type creation and column addition)
    - Migration SQL contains `transcode_error` (text column addition)
    - Migration SQL contains `deleted_at` (timestamp column addition)
    - Migration SQL contains `DROP NOT NULL` for post_id (making it nullable)
    - Migration SQL contains index creation for `post_media_deleted_at`
    - Migration SQL contains index creation for `post_media_transcode_status`
    - `packages/db/drizzle/meta/_journal.json` has 4 entries (idx 0-3), with entry idx=3 tagged "phase-06-media-handling"
    - `packages/db/drizzle/meta/0003_snapshot.json` exists
  </acceptance_criteria>
  <done>Migration SQL file generated by drizzle-kit with all Phase 6 schema changes (transcode_status enum + column, transcode_error, deleted_at, nullable postId, two indexes). Journal and snapshot updated. Gap from 06-VERIFICATION.md Truth #28 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan generates a migration SQL file from existing schema definitions. No runtime code, no user input, no network operations.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | Migration SQL file | accept | Migration is generated by drizzle-kit from version-controlled schema. File is committed to git and reviewed before deployment. No manual SQL editing. |
</threat_model>

<verification>
1. Migration file exists: `test -f packages/db/drizzle/0003_phase-06-media-handling.sql`
2. Contains transcode_status enum: `grep "transcode_status" packages/db/drizzle/0003_phase-06-media-handling.sql`
3. Contains all new columns: grep for transcode_error, deleted_at, DROP NOT NULL
4. Journal updated: `grep "phase-06-media-handling" packages/db/drizzle/meta/_journal.json`
5. Snapshot exists: `test -f packages/db/drizzle/meta/0003_snapshot.json`
</verification>

<success_criteria>
- The gap from 06-VERIFICATION.md (Truth #28: "Schema push applies all pending schema changes to the database") is resolved
- A versioned, idempotent migration SQL file exists for all Phase 6 schema changes
- The migration file can be applied via `drizzle-kit migrate` when a database is available
- No manual SQL was hand-written -- all DDL generated by drizzle-kit from the existing schema TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/06-media-handling/06-06-SUMMARY.md`
</output>
