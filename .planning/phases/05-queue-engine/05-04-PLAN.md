---
phase: 05-queue-engine
plan: 04
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/web/src/pages/queues/QueuesPage.tsx
  - packages/web/src/pages/queues/QueueDetailPage.tsx
  - packages/web/src/hooks/use-queues.ts
  - packages/web/src/components/queues/ScheduleBuilder.tsx
  - packages/web/src/components/queues/HourWindowGrid.tsx
  - packages/web/src/components/queues/DayOfWeekSelector.tsx
  - packages/web/src/components/queues/QueueActionsMenu.tsx
  - packages/web/src/components/queues/QueueStatusBadge.tsx
  - packages/web/src/components/layout/Sidebar.tsx
  - packages/web/src/App.tsx
autonomous: true
requirements:
  - QUEUE-01
  - QUEUE-02
  - QUEUE-03

must_haves:
  truths:
    - "User can see a list of all queues with name, profile, post count, status, last published, and next run"
    - "User can filter queues by network and status"
    - "User can create a new queue with all schedule configuration options via a form"
    - "User can edit an existing queue's schedule configuration"
    - "User can copy a queue's configuration to pre-populate a new queue form"
    - "User can delete a queue with a confirmation dialog"
    - "Sidebar navigation includes Queues link between Posts and New Post"
  artifacts:
    - path: "packages/web/src/pages/queues/QueuesPage.tsx"
      provides: "Queue list page with filters, table, and per-queue actions"
      min_lines: 80
    - path: "packages/web/src/pages/queues/QueueDetailPage.tsx"
      provides: "Queue create/edit form with schedule builder"
      min_lines: 100
    - path: "packages/web/src/components/queues/ScheduleBuilder.tsx"
      provides: "Schedule builder: interval config, days, hours, seasonal window"
      min_lines: 60
    - path: "packages/web/src/hooks/use-queues.ts"
      provides: "TanStack Query hooks for queue CRUD"
      exports: ["useQueues", "useQueue", "useCreateQueue", "useUpdateQueue", "useDeleteQueue"]
  key_links:
    - from: "packages/web/src/pages/queues/QueuesPage.tsx"
      to: "/api/queues"
      via: "TanStack Query hook"
      pattern: "useQueues"
    - from: "packages/web/src/pages/queues/QueueDetailPage.tsx"
      to: "/api/queues"
      via: "TanStack Query mutation hooks"
      pattern: "useCreateQueue|useUpdateQueue"
    - from: "packages/web/src/App.tsx"
      to: "packages/web/src/pages/queues/QueuesPage.tsx"
      via: "React Router route"
      pattern: "path.*queues"
---

<objective>
Build the queue list page (with filter bar, table, per-queue actions) and the queue create/edit page (with the schedule builder form). Add the "Queues" sidebar navigation item and React Router routes.

Purpose: Users need a visual interface to manage their queues -- create new ones with schedule configurations, view all queues in a filterable list, edit/copy/delete queues.
Output: QueuesPage, QueueDetailPage, ScheduleBuilder component, useQueues hooks, sidebar nav update, routes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-queue-engine/05-CONTEXT.md
@.planning/phases/05-queue-engine/05-UI-SPEC.md
@.planning/phases/05-queue-engine/05-02-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 -->
API routes available:
- GET /api/queues -- list queues with ?network=&status= filters
- POST /api/queues -- create queue (body: createQueueSchema)
- GET /api/queues/:id -- get single queue
- PUT /api/queues/:id -- update queue
- DELETE /api/queues/:id -- delete queue
- GET /api/queues/:id/config -- copy config

<!-- Existing frontend patterns -->
From packages/web/src/hooks/use-posts.ts (pattern to mirror):
```typescript
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
// Pattern: export function usePosts(filters) { return useQuery({ queryKey, queryFn, refetchInterval }); }
// Pattern: export function useCreatePost() { return useMutation({ mutationFn, onSuccess: invalidate }); }
```

From packages/web/src/pages/posts/PostsPage.tsx (pattern to mirror):
- Filter bar with Select dropdowns above table
- Table using TanStack React Table
- Skeleton loading (5 rows)
- Empty state with icon + heading + body + CTA
- Per-row kebab DropdownMenu

From packages/web/src/components/layout/Sidebar.tsx:
```typescript
const navItems = [
  { to: '/', icon: LayoutDashboard, label: 'Dashboard', end: true },
  { to: '/posts', icon: FileText, label: 'Posts' },
  { to: '/posts/new', icon: PenSquare, label: 'New Post', isAction: true },
  { to: '/profiles', icon: Users, label: 'Profiles' },
  { to: '/settings', icon: Settings, label: 'Settings' },
  { to: '/admin/queues', icon: Activity, label: 'Admin queues', external: true },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TanStack Query hooks, queue list page with filters and actions, sidebar nav update</name>
  <files>
    packages/web/src/hooks/use-queues.ts,
    packages/web/src/pages/queues/QueuesPage.tsx,
    packages/web/src/components/queues/QueueActionsMenu.tsx,
    packages/web/src/components/queues/QueueStatusBadge.tsx,
    packages/web/src/components/layout/Sidebar.tsx,
    packages/web/src/App.tsx
  </files>
  <read_first>
    packages/web/src/hooks/use-posts.ts,
    packages/web/src/pages/posts/PostsPage.tsx,
    packages/web/src/components/posts/PostActionsMenu.tsx,
    packages/web/src/components/layout/Sidebar.tsx,
    packages/web/src/App.tsx,
    packages/web/src/lib/api-client.ts,
    .planning/phases/05-queue-engine/05-UI-SPEC.md
  </read_first>
  <action>
    1. Create `packages/web/src/hooks/use-queues.ts`:
       - `useQueues(filters: { network?: string; status?: string })` -- TanStack Query with queryKey `['queues', filters]`, fetches `GET /api/queues?network=&status=`, `refetchInterval: 30_000` (UI-SPEC: 30s polling, pauses when hidden)
       - `useQueue(id: string)` -- single queue fetch, queryKey `['queues', id]`
       - `useCreateQueue()` -- useMutation calling `POST /api/queues`, onSuccess invalidates `['queues']`
       - `useUpdateQueue()` -- useMutation calling `PUT /api/queues/:id`, onSuccess invalidates `['queues']` and `['queues', id]`
       - `useDeleteQueue()` -- useMutation calling `DELETE /api/queues/:id`, onSuccess invalidates `['queues']`
       - `useCopyQueueConfig(id: string)` -- useQuery calling `GET /api/queues/:id/config`, enabled: false (manual trigger)
       - All use the existing api-client for CSRF token handling

    2. Create `packages/web/src/components/queues/QueueStatusBadge.tsx`:
       - Props: `{ isPaused: boolean; postCount: number; seasonalStart?: string; seasonalEnd?: string }`
       - Active: `outline` badge with green dot, text "Active"
       - Paused: `secondary` badge with amber dot, text "Paused"
       - Seasonal pause: `secondary` badge with amber dot + Clock icon, text "Seasonal pause"
       - Empty (postCount === 0): `secondary` badge, text "Empty"
       - Per UI-SPEC color roles: success for active, warning for paused/seasonal

    3. Create `packages/web/src/components/queues/QueueActionsMenu.tsx`:
       - Props: `{ queue: QueueListItem; onDelete: () => void; onCopyConfig: () => void }`
       - DropdownMenu (kebab pattern from PostActionsMenu):
         - View Posts: `List` icon, navigates to `/queues/${id}/posts`
         - Edit Queue: `Pencil` icon, navigates to `/queues/${id}/edit`
         - Copy Configuration: `Copy` icon, calls onCopyConfig
         - View Notes: `StickyNote` icon, disabled if queue.notes is null/empty
         - Delete Queue: `Trash2` icon, destructive text, calls onDelete
       - Delete uses AlertDialog: Title "Delete this queue?", Body "This removes the queue and its schedule configuration. Posts in this queue will become unqueued but are not deleted.", Confirm "Delete Queue" (destructive variant)

    4. Create `packages/web/src/pages/queues/QueuesPage.tsx` following PostsPage pattern:
       - Page title: "Queues" (`text-2xl font-semibold`)
       - Header with "Create Queue" primary button (Plus icon), links to `/queues/new`
       - Filter bar above table:
         - Network: Select dropdown (All / Twitter)
         - Status: Select dropdown (All / Active / Paused / Empty)
         - Search: Input with Search icon, client-side filter by queue name
       - Table columns per UI-SPEC:
         - Name: queue name as NavLink to `/queues/${id}/posts` (flex-grow)
         - Profile: network icon + profile name (160px)
         - Posts: total post count integer (80px)
         - Status: QueueStatusBadge component (120px)
         - Last Published: relative timestamp with absolute tooltip (140px)
         - Next Run: relative timestamp, or "Paused"/"Empty" (140px)
         - Actions: QueueActionsMenu kebab (48px)
       - States:
         - Loading: 5 Skeleton rows
         - Empty (no queues): centered icon + "No queues yet" + "Create a queue to schedule recurring posts on autopilot." + "Create Queue" CTA
         - Empty (filtered): "No matching queues" + "Try adjusting your filters."
         - Error: "Failed to load queues" + "Check your connection and try again."
         - Row hover: `bg-muted/50` transition
       - Polling: refetchInterval 30_000 via useQueues hook
       - Copy Configuration action: fetch config via useCopyQueueConfig, navigate to `/queues/new?copyFrom=${id}` with config in state

    5. Update `packages/web/src/components/layout/Sidebar.tsx`:
       - Import `ListOrdered` from lucide-react
       - Add to navItems array between "Posts" and "New Post":
         ```typescript
         { to: '/queues', icon: ListOrdered, label: 'Queues' },
         ```

    6. Update `packages/web/src/App.tsx`:
       - Add React Router routes:
         - `/queues` -> QueuesPage
         - `/queues/new` -> QueueDetailPage
         - `/queues/:id/edit` -> QueueDetailPage
         - `/queues/:id/posts` -> QueuePostsPage (placeholder import, built in Plan 05)
       - Lazy load queue pages for code splitting
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && npx tsc --noEmit -p packages/web/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/hooks/use-queues.ts contains `export function useQueues`
    - packages/web/src/hooks/use-queues.ts contains `refetchInterval: 30_000`
    - packages/web/src/hooks/use-queues.ts contains `export function useCreateQueue`
    - packages/web/src/pages/queues/QueuesPage.tsx contains `No queues yet` (empty state)
    - packages/web/src/pages/queues/QueuesPage.tsx contains `Create Queue`
    - packages/web/src/pages/queues/QueuesPage.tsx contains `Skeleton` (loading state)
    - packages/web/src/components/queues/QueueActionsMenu.tsx contains `Delete this queue?`
    - packages/web/src/components/queues/QueueActionsMenu.tsx contains `Trash2`
    - packages/web/src/components/queues/QueueStatusBadge.tsx contains `Active` and `Paused`
    - packages/web/src/components/layout/Sidebar.tsx contains `ListOrdered`
    - packages/web/src/components/layout/Sidebar.tsx contains `/queues`
    - packages/web/src/App.tsx contains `queues`
    - TypeScript compilation passes for packages/web
  </acceptance_criteria>
  <done>Queue list page shows all queues with filters, table with all specified columns, per-queue actions (view posts, edit, copy config, view notes, delete with confirmation). Sidebar has Queues nav item. Routes registered.</done>
</task>

<task type="auto">
  <name>Task 2: Queue create/edit page with schedule builder form</name>
  <files>
    packages/web/src/pages/queues/QueueDetailPage.tsx,
    packages/web/src/components/queues/ScheduleBuilder.tsx,
    packages/web/src/components/queues/HourWindowGrid.tsx,
    packages/web/src/components/queues/DayOfWeekSelector.tsx
  </files>
  <read_first>
    packages/web/src/pages/posts/NewPostPage.tsx,
    packages/web/src/hooks/use-queues.ts,
    packages/web/src/hooks/use-profiles.ts,
    .planning/phases/05-queue-engine/05-UI-SPEC.md
  </read_first>
  <action>
    1. Create `packages/web/src/components/queues/HourWindowGrid.tsx` (D-01):
       - Props: `{ value: number[]; onChange: (hours: number[]) => void; is24Hour?: boolean }`
       - Grid of checkboxes from 6am to 11pm (18 slots: hours 6 through 23)
       - Layout: 6-column grid on desktop (`grid-cols-6`), 3-column on mobile (`grid-cols-3`)
       - Each checkbox shows the hour formatted per user preference (12h: "6 AM", "7 AM", ..., "11 PM" or 24h: "06", "07", ..., "23")
       - "Select All" / "Clear All" links above the grid
       - Wrap in `<fieldset>` + `<legend>` reading "Hour windows" (accessibility)
       - Each checkbox has visible `<label>` and `aria-label` with full hour name
       - Spacing: `gap-3` (12px) per UI-SPEC declared exception for dense checkbox grid

    2. Create `packages/web/src/components/queues/DayOfWeekSelector.tsx`:
       - Props: `{ value: number[]; onChange: (days: number[]) => void }`
       - 7 checkboxes in a horizontal row: Sun(0), Mon(1), Tue(2), Wed(3), Thu(4), Fri(5), Sat(6)
       - Visible labels: 3-letter abbreviations (Sun, Mon, ...)
       - Each checkbox has `aria-label` with full day name ("Sunday", "Monday", ...)
       - Wrap in `<fieldset>` + `<legend>` reading "Days of week"

    3. Create `packages/web/src/components/queues/ScheduleBuilder.tsx`:
       - Props: RHF `control` and `watch` from React Hook Form (integrated into parent form)
       - Sections (per UI-SPEC interaction states):
         a. **Interval configuration**: row containing interval type Select (Fixed interval / Variable interval), interval value Input (number, min 1), interval unit Select (minutes/hours/days/weeks/months/years). Below the select: helper text that changes based on selection (D-02):
            - Fixed: "Posts go out at clock-aligned times (e.g., every 4 hours = 8am, 12pm, 4pm, 8pm)."
            - Variable: "Timer starts after the previous post publishes (e.g., 4 hours after last publish)."
         b. **Days of week**: DayOfWeekSelector component
         c. **Hour windows**: HourWindowGrid component with helper "Posts only go out during checked hours (in your timezone)."
         d. **Start date**: native `<input type="date">` with label "Start date" and helper "Queue begins publishing on this date. Leave blank to start immediately."
         e. **Seasonal window**: collapsed by default. "Add seasonal window" link expands it. Two date inputs for start MM-DD and end MM-DD, plus "Repeat annually" Switch toggle. Helper "Queue only runs between these dates. Leave blank for year-round."
         f. **Recycling**: Switch toggle with label "Recycle posts" and helper "When all posts have published, start over from the first post." Repeat icon.

    4. Create `packages/web/src/pages/queues/QueueDetailPage.tsx`:
       - React Hook Form with Zod resolver using the createQueueSchema from @sms/shared
       - Page title: "Create Queue" (new) or "Edit Queue" (editing) -- `text-2xl font-semibold`
       - Form layout: single-column inside standard page layout (`p-6 lg:p-8`)
       - Fields in order:
         a. Queue name: Input with label "Queue name", placeholder "e.g., Daily tips, Weekly promos"
         b. Social profile: Select dropdown with label "Social profile", placeholder "Select a profile". Populated from `useProfiles()` hook (existing). Shows network icon + profile name.
         c. ScheduleBuilder component (contains interval, days, hours, start date, seasonal, recycling)
         d. Internal notes: Textarea with label "Internal notes", placeholder "Optional notes about this queue (not published)"
       - Buttons at bottom:
         - Primary: "Create Queue" (new) or "Save Queue" (edit) -- variant="default"
         - Secondary: "Cancel" -- variant="outline", navigates back
       - States per UI-SPEC:
         - Loading (edit mode): Skeleton form fields
         - Saving: Button shows Loader2 spinner, form disabled
         - Success (create): toast "Queue created.", navigate to `/queues/${id}/posts`
         - Success (edit): toast "Queue updated.", navigate to `/queues/${id}/posts`
         - Error: toast "Couldn't save queue. Try again.", form re-enables
         - Validation: inline FormMessage below each invalid field
       - Edit mode: load queue data via `useQueue(id)`, pre-populate form with existing values
       - Copy mode: read `copyFrom` from URL search params, load config via API, pre-populate form fields (but NOT name or profile)
       - Validation messages per UI-SPEC copywriting:
         - "Queue name is required."
         - "Select a social profile."
         - "Interval must be at least 1."
         - "Select at least one day of the week."
         - "Select at least one hour window."
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && npx tsc --noEmit -p packages/web/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/pages/queues/QueueDetailPage.tsx contains `Create Queue` and `Edit Queue`
    - packages/web/src/pages/queues/QueueDetailPage.tsx contains `useCreateQueue` or `useUpdateQueue`
    - packages/web/src/pages/queues/QueueDetailPage.tsx contains `Queue name is required`
    - packages/web/src/components/queues/ScheduleBuilder.tsx contains `Fixed interval` and `Variable interval`
    - packages/web/src/components/queues/ScheduleBuilder.tsx contains `Recycle posts`
    - packages/web/src/components/queues/ScheduleBuilder.tsx contains `seasonal`
    - packages/web/src/components/queues/HourWindowGrid.tsx contains `fieldset` and `legend`
    - packages/web/src/components/queues/HourWindowGrid.tsx contains `Select All`
    - packages/web/src/components/queues/HourWindowGrid.tsx contains `grid-cols-6`
    - packages/web/src/components/queues/DayOfWeekSelector.tsx contains `Sun` and `Mon`
    - packages/web/src/components/queues/DayOfWeekSelector.tsx contains `aria-label`
    - TypeScript compilation passes for packages/web
  </acceptance_criteria>
  <done>Queue create/edit page with full schedule builder form. HourWindowGrid renders 18 hour checkboxes (6-23) in a 6-column grid with accessibility fieldset/legend. DayOfWeekSelector renders 7 day checkboxes. ScheduleBuilder combines all schedule config sections. Form uses RHF + Zod validation with all UI-SPEC copy strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Form data validated client-side by Zod + server-side by API (defense in depth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Client-side form bypass | accept | Client-side Zod validation is UX convenience; server-side validation in Plan 02 is the security boundary. Client-side bypass only affects UX, not data integrity. |
| T-05-04-02 | Information Disclosure | Queue data in browser | accept | Single-user app; the user viewing their own queue data in the browser is expected behavior, not a threat. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p packages/web/tsconfig.json` succeeds
2. Queue list page renders with all specified columns and filter bar
3. Queue create form includes all schedule configuration fields
4. Sidebar shows "Queues" navigation item between "Posts" and "New Post"
</verification>

<success_criteria>
- Queue list page displays all queues with filters, table columns per UI-SPEC, and per-queue actions
- Queue create/edit form includes schedule builder with all D-01/D-02/D-03/D-04 fields
- HourWindowGrid renders 18 checkboxes (6am-11pm) with accessible fieldset/legend
- DayOfWeekSelector renders 7 day checkboxes with full-name aria-labels
- Sidebar includes Queues nav item with ListOrdered icon
- All copywriting matches UI-SPEC exactly
- TypeScript compiles
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-engine/05-04-SUMMARY.md`
</output>
