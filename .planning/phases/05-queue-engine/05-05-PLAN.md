---
phase: 05-queue-engine
plan: 05
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - packages/web/src/pages/queues/QueuePostsPage.tsx
  - packages/web/src/hooks/use-queue-posts.ts
  - packages/web/src/components/queues/QueuePostActionsMenu.tsx
  - packages/web/src/components/queues/SpinnableVariantsDialog.tsx
  - packages/web/src/components/posts/PostStatusBadge.tsx
  - packages/web/src/pages/posts/NewPostPage.tsx
autonomous: false
requirements:
  - QUEUE-04
  - QUEUE-05
  - WORKER-09

must_haves:
  truths:
    - "User can see all posts within a queue ordered by position with per-post actions"
    - "User can reorder posts in a queue via move up/down buttons with optimistic UI update"
    - "User can view spinnable text variants for posts with spin syntax"
    - "User can add a new post to a queue using the adapted post creation form"
    - "Post status badges include auto_destructing and destroyed states"
    - "Queue posts page shows which post is next to publish via cursor indicator"
  artifacts:
    - path: "packages/web/src/pages/queues/QueuePostsPage.tsx"
      provides: "Queue posts list with reorder controls and per-post actions"
      min_lines: 100
    - path: "packages/web/src/hooks/use-queue-posts.ts"
      provides: "TanStack Query hooks for queue posts management"
      exports: ["useQueuePosts", "useMovePostUp", "useMovePostDown", "useRemoveFromQueue"]
    - path: "packages/web/src/components/queues/SpinnableVariantsDialog.tsx"
      provides: "Dialog showing resolved spin syntax variants"
      min_lines: 40
  key_links:
    - from: "packages/web/src/pages/queues/QueuePostsPage.tsx"
      to: "/api/queues/:id/posts"
      via: "TanStack Query hook"
      pattern: "useQueuePosts"
    - from: "packages/web/src/pages/queues/QueuePostsPage.tsx"
      to: "packages/web/src/components/queues/SpinnableVariantsDialog.tsx"
      via: "View Variants kebab action"
      pattern: "SpinnableVariantsDialog"
    - from: "packages/web/src/pages/posts/NewPostPage.tsx"
      to: "/api/queues/:id/posts"
      via: "queue post save handler"
      pattern: "Save to Queue"
---

<objective>
Build the queue posts list page (with reorder controls, cursor indicator, per-post actions), the spinnable variants dialog, the queue post creation flow (adapted NewPostPage), and extend the PostStatusBadge for auto-destruct states. Include a human verification checkpoint for the full UI flow.

Purpose: Users need to manage posts within their queues -- see the ordered list, reorder via move up/down, view spinnable text variants, add new posts, and understand which post publishes next.
Output: QueuePostsPage, useQueuePosts hooks, SpinnableVariantsDialog, QueuePostActionsMenu, adapted NewPostPage for queue mode, extended PostStatusBadge
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-queue-engine/05-CONTEXT.md
@.planning/phases/05-queue-engine/05-UI-SPEC.md
@.planning/phases/05-queue-engine/05-02-SUMMARY.md

<interfaces>
<!-- API endpoints from Plan 02 -->
Queue posts API:
- GET /api/queues/:id/posts -- list posts in queue (ordered by position)
- POST /api/queues/:id/posts -- add post to queue (body: { postId })
- POST /api/queues/:id/posts/:postId/move-up -- reorder up
- POST /api/queues/:id/posts/:postId/move-down -- reorder down
- DELETE /api/queues/:id/posts/:postId -- remove post from queue

<!-- Queue detail from Plan 02 -->
GET /api/queues/:id returns:
```json
{ "id", "name", "profileId", "cursorPosition", "isRecycling", "isPaused", "nextRunAt", ... }
```

<!-- Spinnable text from Plan 01 -->
From packages/shared/src/lib/spinnable-text.ts:
```typescript
export function resolveSpinnableText(text: string): string;
export function countTotalVariants(text: string): number;
```

<!-- Existing post form pattern -->
From packages/web/src/pages/posts/NewPostPage.tsx:
- React Hook Form + Zod resolver
- Text editor with character count
- Spinnable text toggle
- AutoDestructPicker component
- Tags multi-select
- Notes textarea
- Submit button with loading state
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Queue posts page with reorder, post actions, spinnable variants dialog, and adapted post form</name>
  <files>
    packages/web/src/hooks/use-queue-posts.ts,
    packages/web/src/pages/queues/QueuePostsPage.tsx,
    packages/web/src/components/queues/QueuePostActionsMenu.tsx,
    packages/web/src/components/queues/SpinnableVariantsDialog.tsx,
    packages/web/src/components/posts/PostStatusBadge.tsx,
    packages/web/src/pages/posts/NewPostPage.tsx
  </files>
  <read_first>
    packages/web/src/hooks/use-posts.ts,
    packages/web/src/pages/posts/PostsPage.tsx,
    packages/web/src/pages/posts/NewPostPage.tsx,
    packages/web/src/components/posts/PostStatusBadge.tsx,
    packages/web/src/hooks/use-queues.ts,
    packages/web/src/lib/api-client.ts,
    .planning/phases/05-queue-engine/05-UI-SPEC.md
  </read_first>
  <action>
    1. Create `packages/web/src/hooks/use-queue-posts.ts`:
       - `useQueuePosts(queueId: string)` -- TanStack Query with queryKey `['queue-posts', queueId]`, fetches `GET /api/queues/${queueId}/posts`, `refetchInterval: 10_000` (UI-SPEC: 10s polling, pauses when hidden)
       - `useMovePostUp(queueId: string)` -- useMutation calling `POST /api/queues/${queueId}/posts/${postId}/move-up`, onMutate: optimistic update swapping position in the query cache, onError: revert cache + toast "Couldn't reorder post. Try again.", onSuccess: invalidate query + toast "Post moved."
       - `useMovePostDown(queueId: string)` -- same as above for move-down
       - `useRemoveFromQueue(queueId: string)` -- useMutation calling `DELETE /api/queues/${queueId}/posts/${postId}`, onSuccess: invalidate
       - `useAddToQueue(queueId: string)` -- useMutation calling `POST /api/queues/${queueId}/posts` with body `{ postId }`

    2. Create `packages/web/src/components/queues/SpinnableVariantsDialog.tsx`:
       - Props: `{ postText: string; open: boolean; onOpenChange: (open: boolean) => void }`
       - Import `resolveSpinnableText`, `countTotalVariants` from `@sms/shared`
       - Dialog content:
         - Title: "Spinnable Text Variants"
         - Description: "{totalVariants} possible combinations from spin syntax." (using `countTotalVariants(postText)`)
         - Body: heading "Sample variants" followed by 5 Card components, each containing `resolveSpinnableText(postText)` result in `text-sm whitespace-pre-wrap`
         - "Regenerate" button: re-runs 5 resolutions and updates state
         - "Close" button
       - If no spin syntax detected (countTotalVariants returns 1): show fallback -- "No spinnable text" heading, "This post doesn't contain {curly brace} spin syntax." body
       - Focus management handled by Radix Dialog (default)

    3. Create `packages/web/src/components/queues/QueuePostActionsMenu.tsx`:
       - Props: `{ post: QueuePost; queueId: string; isRecycling: boolean; cursorPosition: number; totalPosts: number; onMoveUp: () => void; onMoveDown: () => void; onDelete: () => void; onViewVariants: () => void }`
       - DropdownMenu kebab pattern:
         - Edit Post: `Pencil` icon, navigates to `/posts/${post.id}/edit`, only when status is 'queued' (STATE-02)
         - View Full Text: `FileText` icon, always available (show in a Dialog or navigate)
         - View Variants: `Shuffle` icon, only when `hasSpinnableText === true`, calls onViewVariants
         - View History: `History` icon, always available, opens existing PostHistoryDialog
         - Move Up: `ChevronUp` icon, disabled when position is 1 or status is not 'queued', `aria-label="Move post to position ${position - 1}"`
         - Move Down: `ChevronDown` icon, disabled when position is last or status is not 'queued', `aria-label="Move post to position ${position + 1}"`
         - Delete Post: `Trash2` icon, destructive text, available when status is 'queued' or 'published' (DELETABLE_STATES for queue context)
       - Delete confirmation: AlertDialog with title "Delete this post?", body "This removes the post from the queue. This cannot be undone.", confirm "Delete Post" (destructive), cancel "Cancel"

    4. Create `packages/web/src/pages/queues/QueuePostsPage.tsx`:
       - Load queue data via `useQueue(id)` and posts via `useQueuePosts(id)`
       - Page header per UI-SPEC:
         - Queue name as title (`text-2xl font-semibold`)
         - QueueStatusBadge
         - Profile icon + profile name
         - "Next run" timestamp (relative, tooltip absolute)
         - "Add Post" primary button (Plus icon), navigates to `/posts/new?queueId=${id}`
       - Table columns per UI-SPEC:
         - Position: integer (60px). If position === cursorPosition, show `text-xs` "Next" label + `aria-label="Position N (next to publish)"` + left border highlight `border-l-2 border-primary`
         - Reorder: Up/Down arrow buttons (80px). Disabled per UI-SPEC conditions (position 1, last position, non-queued status, publishing status)
         - Text: truncated first 80 chars of post text (flex-grow)
         - Status: PostStatusBadge component (100px)
         - Spin: "Spin" indicator badge if `hasSpinnableText` (60px)
         - Auto-Destruct: duration string from `autoDestructAfter` or "--" (100px)
         - Actions: QueuePostActionsMenu kebab (48px)
       - Row states per UI-SPEC:
         - `publishing` status: reorder disabled, edit disabled, delete disabled
         - `published` (recycling off): `opacity-60`, reorder disabled
         - `published` (recycling on): normal appearance, reorder enabled
       - Reorder interaction: optimistic update via useMovePostUp/Down hooks
       - States: loading (5 Skeleton rows), empty (icon + "No posts in this queue" + "Add posts to start publishing on schedule." + "Add Post" CTA), error
       - Polling: 10s via useQueuePosts hook

    5. Extend `packages/web/src/components/posts/PostStatusBadge.tsx`:
       - Add `auto_destructing` badge: custom destructive tint, text "Auto-destructing"
       - Add `destroyed` badge: custom success tint (muted), text "Destroyed"
       - Per UI-SPEC badge mapping:
         - auto_destructing: destructive/muted color role
         - destroyed: success (muted) color role -- green tint because destruction was the intended outcome

    6. Adapt `packages/web/src/pages/posts/NewPostPage.tsx` for queue mode (D-09, D-10):
       - Read `queueId` from URL search params (`/posts/new?queueId=xxx`)
       - When `queueId` is present:
         a. Load queue data via `useQueue(queueId)` to get the assigned profileId
         b. Hide profile selector -- pre-fill from queue's profileId
         c. Hide schedule date/time picker
         d. Hide "Publish Now" split button option
         e. Hide "Save as Draft" split button option
         f. Change submit button to single `Button variant="default"` reading "Save to Queue"
         g. On submit: create the post via existing `POST /api/posts` with status 'queued', then call `POST /api/queues/${queueId}/posts` with the new post's id to assign to queue
         h. On success: toast "Post added to queue.", navigate back to `/queues/${queueId}/posts`
       - When `queueId` is absent: existing behavior unchanged (scheduled post creation)
       - All other form fields remain: text/thread editor, character count, spinnable text toggle, auto-destruct picker, tags, notes
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && npx tsc --noEmit -p packages/web/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - packages/web/src/hooks/use-queue-posts.ts contains `export function useQueuePosts`
    - packages/web/src/hooks/use-queue-posts.ts contains `export function useMovePostUp`
    - packages/web/src/hooks/use-queue-posts.ts contains `refetchInterval: 10_000`
    - packages/web/src/pages/queues/QueuePostsPage.tsx contains `No posts in this queue` (empty state)
    - packages/web/src/pages/queues/QueuePostsPage.tsx contains `Add Post`
    - packages/web/src/pages/queues/QueuePostsPage.tsx contains `border-l-2 border-primary` (cursor indicator)
    - packages/web/src/pages/queues/QueuePostsPage.tsx contains `next to publish` (accessibility label)
    - packages/web/src/pages/queues/QueuePostsPage.tsx contains `Skeleton` (loading state)
    - packages/web/src/components/queues/SpinnableVariantsDialog.tsx contains `Spinnable Text Variants`
    - packages/web/src/components/queues/SpinnableVariantsDialog.tsx contains `Regenerate`
    - packages/web/src/components/queues/SpinnableVariantsDialog.tsx contains `countTotalVariants`
    - packages/web/src/components/queues/QueuePostActionsMenu.tsx contains `ChevronUp` and `ChevronDown`
    - packages/web/src/components/queues/QueuePostActionsMenu.tsx contains `aria-label`
    - packages/web/src/components/posts/PostStatusBadge.tsx contains `auto_destructing`
    - packages/web/src/components/posts/PostStatusBadge.tsx contains `destroyed`
    - packages/web/src/pages/posts/NewPostPage.tsx contains `Save to Queue`
    - packages/web/src/pages/posts/NewPostPage.tsx contains `queueId`
    - TypeScript compilation passes for packages/web
  </acceptance_criteria>
  <done>Queue posts page shows all posts with reorder controls, cursor indicator, and per-post actions. Spinnable variants dialog resolves and displays 5 random variants. PostStatusBadge handles auto_destructing and destroyed states. NewPostPage adapts for queue mode with hidden scheduling fields and "Save to Queue" button.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete queue engine UI</name>
  <files>packages/web/src/pages/queues/QueuesPage.tsx, packages/web/src/pages/queues/QueueDetailPage.tsx, packages/web/src/pages/queues/QueuePostsPage.tsx</files>
  <action>
    Human verifies the complete queue engine UI flow end-to-end. All automated implementation is complete; this checkpoint confirms visual correctness, interaction behavior, and copy accuracy against the UI-SPEC.

    What was built:
    - Queue list page with filters and actions
    - Queue create/edit form with schedule builder (hour windows, days of week, interval type, seasonal window, recycling)
    - Queue posts page with reorder buttons and cursor indicator
    - Spinnable variants dialog
    - Adapted post creation form for queue mode
    - Sidebar navigation update
    - Auto-destruct/destroyed status badges

    Steps to verify:
    1. Navigate to the app in the browser
    2. Verify "Queues" appears in the sidebar between "Posts" and "New Post" with the ListOrdered icon
    3. Click "Queues" -- verify the empty state: "No queues yet" with "Create Queue" button
    4. Click "Create Queue" -- verify the form includes all schedule config fields
    5. Fill out the form and create a queue -- verify success toast and redirect
    6. Go back to queue list -- verify the new queue appears with all columns
    7. Test per-queue actions: Edit, Copy Configuration, Delete (confirmation dialog)
    8. Navigate to queue posts page -- verify empty state with "Add Post" button
    9. Click "Add Post" -- verify the post form hides scheduling fields and shows "Save to Queue"
    10. Verify reorder buttons (up/down) appear and are properly disabled at boundaries
    11. If any post has spinnable text, verify "View Variants" dialog
  </action>
  <verify>
    <automated>echo "Human verification checkpoint -- no automated command"</automated>
  </verify>
  <done>Human has verified all queue engine UI pages render correctly, interactions work as specified in UI-SPEC, and copy matches the copywriting contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | All mutations go through authenticated API; UI is a presentation layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | Optimistic reorder bypass | accept | Optimistic updates are UX convenience; server validates ownership and position within transaction. If optimistic update fails, cache reverts. |
| T-05-05-02 | Information Disclosure | Spinnable text resolution in browser | accept | Resolution happens client-side for preview only. The actual publish-time resolution happens server-side in the worker. No security-sensitive data in spin syntax. |
</threat_model>

<verification>
1. `npx tsc --noEmit -p packages/web/tsconfig.json` succeeds
2. Queue posts page renders with position column, reorder buttons, status badges, cursor indicator
3. Spinnable variants dialog shows resolved variants with regenerate functionality
4. NewPostPage in queue mode hides scheduling fields and shows "Save to Queue"
5. PostStatusBadge renders auto_destructing and destroyed states correctly
6. Human verification checkpoint confirms full UI flow works end-to-end
</verification>

<success_criteria>
- Queue posts list shows all posts ordered by position with reorder controls
- Move up/down uses optimistic updates with error rollback
- Cursor indicator highlights the next-to-publish post with border and "Next" label
- Spinnable variants dialog shows 5 variants with regenerate button
- Post creation form adapts for queue mode: no scheduling fields, "Save to Queue" button
- PostStatusBadge includes auto_destructing and destroyed variants
- All UI copy matches UI-SPEC exactly
- Human verification confirms visual correctness
</success_criteria>

<output>
After completion, create `.planning/phases/05-queue-engine/05-05-SUMMARY.md`
</output>
