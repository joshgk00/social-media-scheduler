---
phase: 06.1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - nginx/nginx.conf
  - Dockerfile
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06
  - AUTH-07

must_haves:
  truths:
    - "docker-compose.yml api service environment block contains SESSION_SECRET: ${SESSION_SECRET} (no :- default)"
    - "Dockerfile defines a web-production stage based on nginx:1.27-alpine that COPYs packages/web/dist into /usr/share/nginx/html"
    - "nginx service in docker-compose.yml uses build.target: web-production (not image: nginx:1.27-alpine)"
    - "web_dist named volume is removed from both the nginx service volumes list and the top-level volumes: block"
    - "nginx.conf location /api/ uses proxy_pass http://api_backend; (no trailing slash) so Express receives the original /api/* path"
    - "nginx.conf defines a location /assets/ block with Cache-Control: public, immutable for Vite-hashed static assets"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Complete api env block + nginx service built from image, not volume"
      contains: "SESSION_SECRET:"
    - path: "Dockerfile"
      provides: "web-production multi-stage target serving built web assets via nginx"
      contains: "AS web-production"
    - path: "nginx/nginx.conf"
      provides: "Fixed proxy_pass preserving /api/ prefix + cache headers for /assets/"
      contains: "proxy_pass http://api_backend;"
  key_links:
    - from: "docker-compose.yml api.environment"
      to: "packages/api/src/index.ts:22 requireEnv('SESSION_SECRET')"
      via: "env var injection at container start"
      pattern: "SESSION_SECRET: \\$\\{SESSION_SECRET\\}"
    - from: "Dockerfile web-production stage"
      to: "docker-compose.yml nginx service"
      via: "build.target web-production"
      pattern: "target: web-production"
    - from: "nginx.conf /api/ location"
      to: "Express routes defined with /api/ prefix"
      via: "proxy_pass http://api_backend (no trailing slash)"
      pattern: "proxy_pass http://api_backend;"
---

<objective>
Three tightly-coupled infrastructure fixes that make the production Docker Compose stack boot and serve both the SPA and API:

1. Add `SESSION_SECRET` to the api service env block so `requireEnv('SESSION_SECRET')` in `packages/api/src/index.ts:22` does not crash the container on startup (closes audit gap INT-02).
2. Create a `web-production` Dockerfile target that bakes built web assets into a custom nginx image, and switch the compose nginx service to build from it — replacing the empty `web_dist` named volume (closes audit gap FLOW-02).
3. Remove the trailing slash from `proxy_pass` in `nginx/nginx.conf:45` so Express receives `/api/*` paths (closes audit gap FLOW-01).

Purpose: Unblock AUTH-01..07 (session-dependent login / logout / 2FA / password / security questions all require SESSION_SECRET at boot) and restore end-to-end API and frontend reachability through nginx.

Output: Three modified files — `docker-compose.yml`, `Dockerfile`, `nginx/nginx.conf` — that together produce a stack where `docker compose up --build` starts cleanly and `/api/*` + `/` both return non-404 responses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-production-deployment-wiring/06.1-RESEARCH.md
@.planning/phases/06.1-production-deployment-wiring/06.1-PATTERNS.md
@docker-compose.yml
@Dockerfile
@nginx/nginx.conf
@packages/api/src/index.ts

<interfaces>
<!-- Key infrastructure contracts extracted from the codebase. -->

Existing api service env block (`docker-compose.yml` lines 33-38) — pattern for required secret (no default):
```yaml
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-scheduler}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-scheduler}
      REDIS_URL: redis://:${REDIS_PASSWORD:-changeme}@redis:6379
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      CSRF_SECRET: ${CSRF_SECRET}
      NODE_ENV: production
```

Existing build-with-target pattern (`docker-compose.yml` lines 27-30):
```yaml
  api:
    build:
      context: .
      target: api-production
```

Existing healthcheck wget pattern (`docker-compose.yml` lines 44-48, mirrored for nginx at 77-81):
```yaml
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/health"]
      interval: 15s
      timeout: 5s
      retries: 3
```

Express route entry point consuming SESSION_SECRET (`packages/api/src/index.ts:22`):
```typescript
const SESSION_SECRET = requireEnv('SESSION_SECRET');
```

Current broken proxy_pass (`nginx/nginx.conf:45`):
```nginx
            proxy_pass http://api_backend/;
```

Existing `build` stage output path (`Dockerfile:31-33`) — already produces `/app/packages/web/dist`:
```dockerfile
FROM install AS build
COPY . .
RUN pnpm -r build
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add SESSION_SECRET to docker-compose.yml api service env block</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (full file, currently 87 lines)
    - packages/api/src/index.ts (line 22 to confirm the env var name)
    - .env.example (if present) — confirm whether SESSION_SECRET is already documented for operators
  </read_first>
  <behavior>
    - After edit: `grep -n "SESSION_SECRET" docker-compose.yml` returns exactly one match inside the api service environment block
    - After edit: `docker compose config --quiet` exits 0 (with SESSION_SECRET defined in `.env` or shell env)
  </behavior>
  <action>
    Edit `docker-compose.yml`. Inside the `api.environment` block (currently lines 33-38), add a new line immediately AFTER `CSRF_SECRET: ${CSRF_SECRET}` and BEFORE `NODE_ENV: production`:

    ```yaml
          SESSION_SECRET: ${SESSION_SECRET}
    ```

    Colocate with `CSRF_SECRET` — both are auth/session secrets (PATTERNS.md "Required-secret env var" pattern). Use `${SESSION_SECRET}` with NO `:-default` fallback so the operator-forgot-to-set-it case fails loudly at `docker compose config` time rather than silently booting with a weak or empty secret.

    Do NOT:
    - Add `MEDIA_STORAGE_BACKEND` or `MEDIA_DIR` — RESEARCH.md anti-patterns forbids this; defaults are correct
    - Add a default value (`:-something`) — that would mask the operator error
    - Modify the worker service env block (the worker does not use sessions)
    - Reorder existing env vars
  </action>
  <acceptance_criteria>
    - `grep -n "SESSION_SECRET: \${SESSION_SECRET}" docker-compose.yml` returns exactly one match
    - That match line number is between the `CSRF_SECRET` line and the `NODE_ENV: production` line inside the `api.environment` block (verified by `grep -n -B1 -A1 "SESSION_SECRET" docker-compose.yml`)
    - `grep -c "MEDIA_STORAGE_BACKEND\|MEDIA_DIR" docker-compose.yml` returns 0
    - `SESSION_SECRET=test docker compose config --quiet` exits 0
    - Worker service env block is unchanged (verified by `grep -c "SESSION_SECRET" docker-compose.yml` returning 1, not 2)
  </acceptance_criteria>
  <verify>
    <automated>SESSION_SECRET=test POSTGRES_PASSWORD=test ENCRYPTION_KEY=0123456789abcdef0123456789abcdef CSRF_SECRET=test docker compose config --quiet</automated>
  </verify>
  <done>
    - `docker-compose.yml` api service receives `SESSION_SECRET` from the operator's `.env` or shell env
    - `docker compose config` validates cleanly
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add web-production Dockerfile stage and switch nginx compose service to build from it</name>
  <files>Dockerfile, docker-compose.yml</files>
  <read_first>
    - Dockerfile (full file, currently 62 lines) — study `api-production` stage (lines 44-52) as analog
    - docker-compose.yml (lines 67-81 — current nginx service + line 86 web_dist volume)
    - .planning/phases/06.1-production-deployment-wiring/06.1-PATTERNS.md (Dockerfile section — note the omit-USER caveat)
    - .planning/phases/06.1-production-deployment-wiring/06.1-RESEARCH.md (Open Question O1 and Pitfall 5)
  </read_first>
  <behavior>
    - After edit: Dockerfile contains a `FROM nginx:1.27-alpine AS web-production` stage
    - After edit: That stage installs wget (needed by the existing healthcheck), copies `nginx/nginx.conf`, and copies `/app/packages/web/dist` from the `build` stage into `/usr/share/nginx/html`
    - After edit: That stage does NOT contain a `USER appuser` directive (nginx base image handles its own user on port-80 bind — per PATTERNS.md caveat)
    - After edit: `docker-compose.yml` nginx service uses `build: { context: ., target: web-production }` instead of `image: nginx:1.27-alpine`
    - After edit: The nginx service `volumes:` list is removed entirely (assets are now baked into the image; nginx.conf comes from the COPY in the Dockerfile stage)
    - After edit: The top-level `volumes:` block no longer contains `web_dist:`
    - After edit: `docker compose config --quiet` still exits 0
  </behavior>
  <action>
    **Step A — Dockerfile** (`Dockerfile`): Append a new stage to the end of the file (after the existing `worker-production` stage at line 62):

    ```dockerfile

    # Web production image (nginx + built SPA assets)
    FROM nginx:1.27-alpine AS web-production
    RUN apk add --no-cache wget
    COPY nginx/nginx.conf /etc/nginx/nginx.conf
    COPY --from=build /app/packages/web/dist /usr/share/nginx/html
    EXPOSE 80
    ```

    Do NOT add `USER appuser` to this stage. Per PATTERNS.md "Dockerfile section" and RESEARCH.md Open Question O1: nginx:1.27-alpine's default entrypoint drops to the `nginx` user after binding port 80 on its own; explicitly setting `USER appuser` breaks the port-80 bind. The `wget` install is required by the existing compose healthcheck (`wget --spider`) which is not present in the alpine nginx image by default.

    Do NOT add a `CMD` line — the `nginx:1.27-alpine` base image supplies the correct default (`nginx -g 'daemon off;'`).

    **Step B — docker-compose.yml** (`docker-compose.yml`): Replace the entire current nginx service block (currently lines 67-81):

    ```yaml
      nginx:
        image: nginx:1.27-alpine
        ports:
          - "${NGINX_PORT:-8080}:80"
        volumes:
          - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
          - web_dist:/usr/share/nginx/html:ro
        depends_on:
          api:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "wget", "--spider", "-q", "http://localhost:80/health"]
          interval: 15s
          timeout: 5s
          retries: 3
    ```

    With:

    ```yaml
      nginx:
        build:
          context: .
          target: web-production
        ports:
          - "${NGINX_PORT:-8080}:80"
        depends_on:
          api:
            condition: service_healthy
        healthcheck:
          test: ["CMD", "wget", "--spider", "-q", "http://localhost:80/health"]
          interval: 15s
          timeout: 5s
          retries: 3
    ```

    Keep the healthcheck verbatim (the `wget` install in Step A makes it work). Drop the `volumes:` list entirely.

    **Step C — top-level volumes block** (`docker-compose.yml`): Remove the `web_dist:` line from the top-level `volumes:` block (currently line 86). Final volumes block must read:

    ```yaml
    volumes:
      postgres_data:
      redis_data:
      media_data:
    ```

    Do NOT touch `postgres_data`, `redis_data`, or `media_data`.

    Do NOT modify `docker-compose.dev.yml` — dev uses Vite dev server, not the production nginx image.
  </action>
  <acceptance_criteria>
    - `grep -c "^FROM nginx:1.27-alpine AS web-production" Dockerfile` returns 1
    - `grep "COPY --from=build /app/packages/web/dist /usr/share/nginx/html" Dockerfile` matches
    - `grep "apk add --no-cache wget" Dockerfile` matches inside the web-production stage (verified by context grep `grep -n -A3 "AS web-production" Dockerfile`)
    - `grep -c "USER appuser" Dockerfile` returns 2 (api-production and worker-production only — NOT the new web-production stage)
    - `grep -c "image: nginx:1.27-alpine" docker-compose.yml` returns 0 (nginx service switched to build)
    - `grep -n "target: web-production" docker-compose.yml` returns one match inside the nginx service block
    - `grep -c "web_dist" docker-compose.yml` returns 0 (removed from both service volumes list and top-level volumes block)
    - `grep -c "^volumes:" docker-compose.yml` returns 1 (top-level block still present)
    - Top-level volumes block contains exactly `postgres_data`, `redis_data`, `media_data` — verified by `grep -n -A4 "^volumes:" docker-compose.yml`
    - `SESSION_SECRET=test POSTGRES_PASSWORD=test ENCRYPTION_KEY=0123456789abcdef0123456789abcdef CSRF_SECRET=test docker compose config --quiet` exits 0
  </acceptance_criteria>
  <verify>
    <automated>SESSION_SECRET=test POSTGRES_PASSWORD=test ENCRYPTION_KEY=0123456789abcdef0123456789abcdef CSRF_SECRET=test docker compose config --quiet</automated>
  </verify>
  <done>
    - `Dockerfile` defines `web-production` target that bakes built web assets + nginx.conf into a custom nginx image
    - `docker-compose.yml` nginx service builds from that target; obsolete `web_dist` named volume is gone
    - Compose config validates cleanly
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix nginx proxy_pass trailing slash and add /assets/ cache headers</name>
  <files>nginx/nginx.conf</files>
  <read_first>
    - nginx/nginx.conf (full file, currently 62 lines)
    - .planning/phases/06.1-production-deployment-wiring/06.1-RESEARCH.md (Pattern 2 and Pitfall 4)
    - packages/api/src/routes/ (sample one or two files to confirm Express routes start with `/api/`)
  </read_first>
  <behavior>
    - After edit: `grep "proxy_pass http://api_backend" nginx/nginx.conf` shows NO trailing slash on the `/api/` location (trailing slash preserved only on `/health` which needs path rewrite)
    - After edit: A `location /assets/` block exists with `Cache-Control "public, immutable"` and `expires 1y`
    - After edit: The /assets/ block is ORDERED before the catch-all `location /` block so nginx matches it first
  </behavior>
  <action>
    **Step A — Fix proxy_pass** (`nginx/nginx.conf` line 45): Delete the trailing slash. Change:

    ```nginx
                proxy_pass http://api_backend/;
    ```

    To:

    ```nginx
                proxy_pass http://api_backend;
    ```

    Do NOT touch the `/health` location block (lines 36-42). Its `proxy_pass http://api_backend/health;` is correct — it explicitly rewrites the request to `/health` (Express health router is mounted without the `/api/` prefix).

    **Step B — Add /assets/ cache block** (`nginx/nginx.conf`): Insert a new location block AFTER the closing `}` of `location /api/` (currently ends at line 55) and BEFORE `location /` (currently starts at line 57). The new block is:

    ```nginx
            location /assets/ {
                root /usr/share/nginx/html;
                expires 1y;
                add_header Cache-Control "public, immutable";
                access_log off;
            }
    ```

    Rationale: Vite emits hashed filenames under `/assets/`, so `immutable` caching is safe and required by CLAUDE.md root ("Production nginx: gzip + static asset cache headers"). Matching `/assets/` before the catch-all `location /` prevents the `try_files $uri $uri/ /index.html` fallback from intercepting asset requests.

    Do NOT:
    - Modify the gzip settings (already present, correct)
    - Change `client_max_body_size` (already set to 100m for media uploads)
    - Touch the `upstream api_backend` block
    - Modify any `proxy_set_header` lines inside `/api/` — only the `proxy_pass` URI changes
  </action>
  <acceptance_criteria>
    - `grep -c "proxy_pass http://api_backend/;" nginx/nginx.conf` returns 0 (the buggy trailing slash in /api/ is gone)
    - `grep -c "proxy_pass http://api_backend;" nginx/nginx.conf` returns 1 (the fixed /api/ location)
    - `grep -c "proxy_pass http://api_backend/health;" nginx/nginx.conf` returns 1 (the /health location is unchanged)
    - `grep -n "location /assets/" nginx/nginx.conf` returns exactly one match
    - `grep "Cache-Control \"public, immutable\"" nginx/nginx.conf` matches inside the /assets/ block
    - The /assets/ block appears on a line number BEFORE the `location /` block (verified by `grep -n "location /" nginx/nginx.conf` — /assets/ line < / line)
    - `nginx -t -c $(pwd)/nginx/nginx.conf` (run via `docker run --rm -v $(pwd)/nginx/nginx.conf:/etc/nginx/nginx.conf:ro nginx:1.27-alpine nginx -t`) exits 0 with "syntax is ok" and "test is successful"
  </acceptance_criteria>
  <verify>
    <automated>docker run --rm -v "$(pwd)/nginx/nginx.conf:/etc/nginx/nginx.conf:ro" nginx:1.27-alpine nginx -t</automated>
  </verify>
  <done>
    - `/api/*` requests through nginx preserve the `/api/` prefix and reach Express routes correctly
    - Vite-hashed asset requests get immutable caching headers
    - `nginx -t` reports the config valid
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator .env -> container env | `SESSION_SECRET`, `CSRF_SECRET`, `ENCRYPTION_KEY`, `POSTGRES_PASSWORD`, `REDIS_PASSWORD` flow from .env into process env at compose up |
| Public network -> nginx:80 | Untrusted HTTP requests enter here; nginx strips or preserves path components before forwarding to the api upstream |
| nginx -> api upstream (internal docker network) | Trusted intra-network traffic; api_backend is not exposed on the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-01 | Information Disclosure | SESSION_SECRET handling | mitigate | No `:-default` fallback in compose — missing env fails loudly at `docker compose config`, preventing silent boot with an empty/predictable secret. Operator must populate `.env` explicitly. (ASVS V3 Session Management) |
| T-06.1-02 | Spoofing | nginx -> api forwarded headers | accept | `X-Forwarded-For`, `X-Real-IP`, `X-Forwarded-Proto`, `X-Request-ID` already set in the existing `/api/` location block. No change to header handling in this plan. |
| T-06.1-03 | Tampering | Static asset immutable caching | mitigate | `/assets/` block applies `immutable` only to Vite-hashed filenames under `/assets/` — content-hash guarantees cache-safety. `index.html` (served by `location /`) remains revalidated via nginx defaults. |
| T-06.1-04 | Denial of Service | Dockerfile web-production non-root binding | accept | Port 80 bind requires the nginx process to start as root on Alpine; the base image's default entrypoint drops privileges to the `nginx` user post-bind. Skipping an explicit `USER appuser` is required for correctness, not a weakening — documented in PATTERNS.md and RESEARCH.md Open Question O1. |
</threat_model>

<verification>
- `SESSION_SECRET=test POSTGRES_PASSWORD=test ENCRYPTION_KEY=0123456789abcdef0123456789abcdef CSRF_SECRET=test docker compose config --quiet` exits 0
- `docker run --rm -v "$(pwd)/nginx/nginx.conf:/etc/nginx/nginx.conf:ro" nginx:1.27-alpine nginx -t` exits 0
- Diffs show only the changes specified per task — no unrelated edits
</verification>

<success_criteria>
- Production compose stack boots without the API container crashing on missing `SESSION_SECRET`
- nginx container builds from the new `web-production` Dockerfile target and serves baked web assets (no empty `web_dist` volume)
- `/api/*` requests through nginx reach Express with the `/api/` prefix intact
- Vite-hashed assets under `/assets/` receive immutable cache headers
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-production-deployment-wiring/06.1-02-SUMMARY.md` covering:
- Diff summary for each of the three files
- Confirmation that `docker compose config --quiet` validates and `nginx -t` passes
- Any deviations from the plan (expected: none)
</output>
