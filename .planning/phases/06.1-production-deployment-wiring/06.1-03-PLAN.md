---
phase: 06.1
plan: 03
type: execute
wave: 2
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md
  - .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md
autonomous: false
requirements:
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - LIMIT-04
  - LIMIT-05

must_haves:
  truths:
    - "04-04-SUMMARY.md frontmatter claims SCHED-03 and LIMIT-04 via a requirements_satisfied: list"
    - "04-05-SUMMARY.md frontmatter claims SCHED-01, SCHED-02, and LIMIT-05 via a requirements_satisfied: list"
    - "Human operator has verified the full production stack boots and routes work end-to-end (docker compose up --build succeeds, /api/health returns 200 through nginx, / returns HTML)"
  artifacts:
    - path: ".planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md"
      provides: "Frontmatter claim for SCHED-03, LIMIT-04"
      contains: "requirements_satisfied"
    - path: ".planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md"
      provides: "Frontmatter claim for SCHED-01, SCHED-02, LIMIT-05"
      contains: "requirements_satisfied"
  key_links:
    - from: "04-04-SUMMARY.md requirements_satisfied"
      to: "ROADMAP Phase 4 requirements list"
      via: "audit tool grep"
      pattern: "SCHED-03|LIMIT-04"
    - from: "04-05-SUMMARY.md requirements_satisfied"
      to: "ROADMAP Phase 4 requirements list"
      via: "audit tool grep"
      pattern: "SCHED-01|SCHED-02|LIMIT-05"
---

<objective>
Two outcomes for phase completion:

1. **Integration checkpoint (human verification)** — With Plans 01 and 02 merged, the full production Docker Compose stack must actually boot, serve the frontend, and route API traffic through nginx to a non-crashing API container. This is the FLOW-01 / FLOW-02 manual verification from 06.1-VALIDATION.md; it cannot be automated without a running Docker daemon and real container orchestration.
2. **Phase 4 SUMMARY frontmatter fix** — Five Phase 4 requirements (SCHED-01, SCHED-02, SCHED-03, LIMIT-04, LIMIT-05) were implemented but not claimed in SUMMARY frontmatter, so the v1.0 audit tool flagged them as missing. Add a `requirements_satisfied:` list to the two relevant SUMMARY files so the audit recognizes coverage. This is a documentation-only change — no source code touched.

Purpose: Close Phase 6.1 cleanly by proving the wiring works end-to-end and correcting the documentation gap identified by the audit.

Output: Two modified SUMMARY markdown files and a human sign-off that the stack is operational.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-production-deployment-wiring/06.1-RESEARCH.md
@.planning/phases/06.1-production-deployment-wiring/06.1-PATTERNS.md
@.planning/phases/06.1-production-deployment-wiring/06.1-VALIDATION.md
@.planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md
@.planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md

<interfaces>
<!-- Frontmatter patterns extracted from the files to be modified. -->

Current `04-04-SUMMARY.md` frontmatter (lines 1-7):
```yaml
---
phase: 04
plan: 4
subsystem: api
tags: [api, routes, rate-limit, bull-board, limit-02, sched-03, sched-04, worker-07]
requires:
  - 04-01 (posts, post_attempts, social_profiles rate-limit columns)
```

Requirement mapping (from PATTERNS.md "04-04-SUMMARY.md + 04-05-SUMMARY.md" section and ROADMAP Phase 4):
- **04-04** delivered: `POST /api/posts/:id/retry`, `GET /api/posts/:id/history`, Twitter pre-flight budget check on `POST /api/posts` and `PATCH /api/posts/:id` → claims **SCHED-03** (per-post actions) and **LIMIT-04** (pre-flight on new Twitter post)
- **04-05** delivered: Posts list page with error column, PostActionsMenu, PostHistoryDialog, polling indicator, RateLimitBanner / Block / Settings UI → claims **SCHED-01** (list view), **SCHED-02** (row fields), **LIMIT-05** (pre-flight on CSV bulk upload — surfaced in the UI banner before bulk submit)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human verification of end-to-end production stack</name>
  <files>(manual verification — no files modified)</files>
  <what-built>
    Plans 06.1-01 and 06.1-02 together:
    - API entry point now constructs storage + transcodeQueue so media routes mount (`packages/api/src/index.ts`)
    - docker-compose api service env block includes `SESSION_SECRET` so the API boots
    - Dockerfile gained a `web-production` stage that bakes built React assets into a custom nginx image
    - docker-compose nginx service switched to build from that target; obsolete `web_dist` volume removed
    - nginx.conf `/api/` proxy_pass no longer strips the `/api/` prefix; `/assets/` gets immutable cache headers
  </what-built>
  <read_first>
    - .env (confirm operator populated SESSION_SECRET, CSRF_SECRET, ENCRYPTION_KEY, POSTGRES_PASSWORD, REDIS_PASSWORD)
    - docker-compose.yml (post-Plan-02 state)
    - Dockerfile (post-Plan-02 state — web-production stage should exist)
    - nginx/nginx.conf (post-Plan-02 state — proxy_pass has no trailing slash, /assets/ block present)
  </read_first>
  <action>
    Perform the nine verification steps below in order. This is a human-executed checkpoint — the operator runs `docker compose up --build` and inspects live output. Do NOT skip steps on partial success; respond with `"issue: &lt;details&gt;"` if any single step fails, or `"approved"` only when all nine succeed.
  </action>
  <how-to-verify>
    Ensure `.env` has `SESSION_SECRET`, `CSRF_SECRET`, `ENCRYPTION_KEY`, `POSTGRES_PASSWORD`, `REDIS_PASSWORD` populated (any cryptographically random value ≥32 chars for secrets).

    1. Run from the project root: `docker compose up --build` (first build may take several minutes)
    2. Watch the API container logs for a line like `{"level":"info","msg":"API server listening","port":3000}`. There must be NO stack trace about missing env vars or a crash loop.
    3. In a second terminal, verify nginx is healthy: `docker compose ps` — all five services (postgres, redis, api, worker, nginx) should show `healthy` or `running (healthy)` within ~60 seconds.
    4. Frontend: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/` → expect `200`
    5. Frontend HTML has content: `curl -s http://localhost:8080/ | grep -c "<!doctype"` → expect `1` (case-insensitive grep for the Vite-produced doctype)
    6. API health through nginx: `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/health` → expect `200`
    7. Media route reachable (will return 401 without a session, but NOT 404): `curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/media/items` → expect `401` (or any non-404 — the point is the router is mounted)
    8. Static asset cache header present: `curl -sI http://localhost:8080/assets/$(curl -s http://localhost:8080/ | grep -oE 'assets/[^"]+\.js' | head -1) | grep -i cache-control` → expect `Cache-Control: public, immutable`
    9. Tear down: `docker compose down`

    If any step fails, capture the failing output and respond with "issue:" + details. Do NOT mark approved on partial success.
  </how-to-verify>
  <acceptance_criteria>
    - Operator response to the checkpoint is exactly `approved` (all nine steps passed), OR
    - Operator response starts with `issue:` and Plan 01 / Plan 02 are re-opened to fix the identified defect
    - In the approved case: all five container services are reported healthy; the /, /api/health, /api/media/* endpoints returned their expected status codes; the /assets/ cache header is present
  </acceptance_criteria>
  <verify>
    <automated>MISSING — checkpoint task requires live Docker daemon; automated gate is the operator's "approved" response captured in the execution transcript</automated>
  </verify>
  <resume-signal>Type "approved" if all nine steps pass, or "issue: &lt;details&gt;" otherwise</resume-signal>
  <done>
    - Operator has replied `approved` confirming all nine verification steps passed
    - Phase 6.1 ROADMAP success criteria 1-3 are observably true in the running stack
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add requirements_satisfied frontmatter to Phase 4 SUMMARY files</name>
  <files>.planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md, .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md (frontmatter and `provides:` list — confirm SCHED-03 + LIMIT-04 mapping)
    - .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md (frontmatter and body — confirm SCHED-01 + SCHED-02 + LIMIT-05 mapping)
    - .planning/phases/06.1-production-deployment-wiring/06.1-PATTERNS.md (section "04-04-SUMMARY.md + 04-05-SUMMARY.md" — canonical ID split)
  </read_first>
  <behavior>
    - After edit: `04-04-SUMMARY.md` frontmatter contains a `requirements_satisfied:` YAML list with exactly two items: `SCHED-03` and `LIMIT-04`
    - After edit: `04-05-SUMMARY.md` frontmatter contains a `requirements_satisfied:` YAML list with exactly three items: `SCHED-01`, `SCHED-02`, `LIMIT-05`
    - After edit: No other changes to either file (no source code, no body text edits, no tag changes)
  </behavior>
  <action>
    **Step A — `04-04-SUMMARY.md`**: Insert a new YAML key `requirements_satisfied:` BETWEEN the existing `tags:` line and the existing `requires:` line (currently lines 5 and 6). The inserted block is:

    ```yaml
    requirements_satisfied:
      - SCHED-03
      - LIMIT-04
    ```

    **Step B — `04-05-SUMMARY.md`**: Read the current frontmatter first (may not include a `tags:` line — the file was created with a different template). Insert `requirements_satisfied:` as a top-level key in the frontmatter, immediately BEFORE the closing `---` fence. The inserted block is:

    ```yaml
    requirements_satisfied:
      - SCHED-01
      - SCHED-02
      - LIMIT-05
    ```

    Do NOT:
    - Modify any body content below the frontmatter
    - Change existing frontmatter keys (phase, plan, subsystem, tags, requires, provides, affects, etc.)
    - Add or remove items from the existing `tags:` list
    - Reorder existing lines
    - Touch any other SUMMARY file — the canonical split per PATTERNS.md is: 04-04 owns SCHED-03 + LIMIT-04, 04-05 owns SCHED-01 + SCHED-02 + LIMIT-05
  </action>
  <acceptance_criteria>
    - `grep -c "^requirements_satisfied:" .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md` returns 1
    - `grep -c "^requirements_satisfied:" .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md` returns 1
    - `grep -c "^  - SCHED-03" .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md` returns 1
    - `grep -c "^  - LIMIT-04" .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md` returns 1
    - `grep -c "^  - SCHED-01" .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md` returns 1
    - `grep -c "^  - SCHED-02" .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md` returns 1
    - `grep -c "^  - LIMIT-05" .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md` returns 1
    - The `requirements_satisfied` block in 04-04 is bounded by `tags:` (above) and `requires:` (below) — verified by `grep -n -B2 -A4 "requirements_satisfied" .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md`
    - Neither file changed outside its frontmatter — verified by running `git diff --stat` and `git diff -- .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md` and confirming only the YAML frontmatter lines are new
    - Running the audit-style grep from 06.1-VALIDATION.md manual row: `grep -A5 requirements_satisfied .planning/phases/04-*/*-SUMMARY.md` produces output containing all five IDs (SCHED-01, SCHED-02, SCHED-03, LIMIT-04, LIMIT-05)
  </acceptance_criteria>
  <verify>
    <automated>grep -A5 "^requirements_satisfied:" .planning/phases/04-publish-worker-scheduled-posts/04-04-SUMMARY.md .planning/phases/04-publish-worker-scheduled-posts/04-05-SUMMARY.md | grep -E "SCHED-0[123]|LIMIT-0[45]" | wc -l | awk '$1 == 5 { exit 0 } { exit 1 }'</automated>
  </verify>
  <done>
    - 04-04 claims SCHED-03 and LIMIT-04 in its frontmatter
    - 04-05 claims SCHED-01, SCHED-02, and LIMIT-05 in its frontmatter
    - No source code or body text changes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced. This plan is verification + documentation frontmatter only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-05 | Repudiation | Human sign-off of deployment | mitigate | Checkpoint records the nine specific curl/docker commands the operator must run; reply is captured in the execution transcript per gsd-execute-phase conventions, producing an auditable trail. |
| T-06.1-06 | Tampering | SUMMARY frontmatter edit | accept | Documentation-only change; no attacker-facing surface. Git history preserves prior state. |
</threat_model>

<verification>
- The checkpoint task's nine manual commands all succeed (captured in the human response)
- The grep-based automated verify for Task 2 exits 0
- `git diff --stat .planning/phases/04-publish-worker-scheduled-posts/` shows only two files changed with minimal line count (~3-6 lines each)
</verification>

<success_criteria>
- Production stack boots cleanly end-to-end; all four ROADMAP Phase 6.1 success criteria are observably true
- Phase 4 SUMMARY frontmatter claims all five documentation-gap requirement IDs
- No source code changes in this plan (documentation + verification only)
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-production-deployment-wiring/06.1-03-SUMMARY.md` covering:
- Capture of the operator's responses to the nine checkpoint steps (at least: "all nine passed" + any notable observations like timing or build cache behavior)
- Diffs for both SUMMARY files
- Confirmation that Phase 6.1 ROADMAP success criteria are satisfied
</output>
