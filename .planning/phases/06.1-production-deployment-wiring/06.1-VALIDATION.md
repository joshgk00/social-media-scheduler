---
phase: 6.1
slug: production-deployment-wiring
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-16
---

# Phase 6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.x |
| **Config file** | `packages/api/vitest.config.ts`, `packages/web/vitest.config.ts` |
| **Quick run command** | `pnpm --filter @sms/api vitest run` |
| **Full suite command** | `pnpm -r test -- --run` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @sms/api vitest run`
- **After every plan wave:** Run `pnpm -r test -- --run`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 6.1-01-01 | 01 | 1 | INT-01 (MEDIA-01..08) | — | N/A | smoke | `pnpm --filter @sms/api vitest run` | ✅ existing media route tests | ⬜ pending |
| 6.1-01-02 | 01 | 1 | INT-01 | — | transcodeQueue shutdown cleanup | unit | `pnpm --filter @sms/api vitest run` | ✅ existing | ⬜ pending |
| 6.1-02-01 | 02 | 1 | INT-02 (AUTH-01..07) | T-6.1-01 | SESSION_SECRET env-sourced, never hardcoded | smoke | `docker compose config --quiet` | ❌ manual | ⬜ pending |
| 6.1-03-01 | 03 | 1 | FLOW-02 | — | N/A | manual | `docker compose up --build` + `curl localhost:8080/` | ❌ manual | ⬜ pending |
| 6.1-03-02 | 03 | 1 | FLOW-01 | — | proxy_pass preserves /api/ prefix | manual | `curl localhost:8080/health` (200 via nginx exact-match) and `curl -X POST localhost:8080/api/media/upload` (403 CSRF gate, proving media router mounted) | ❌ manual | ⬜ pending |
| 6.1-04-01 | 04 | 2 | SCHED-01..03, LIMIT-04..05 | — | N/A | manual | `grep requirements_satisfied .planning/phases/04-*/*-SUMMARY.md` | N/A docs | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements. No new test files needed. This phase is wiring existing code, not creating new functionality.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| API starts without crashing | INT-02 | Requires full Docker Compose stack | `docker compose up --build api` — verify no crash in logs |
| nginx serves index.html | FLOW-02 | Requires built web assets in Docker | `docker compose up --build` + `curl http://localhost:8080/` returns HTML |
| API routes reachable through nginx | FLOW-01 | Requires running nginx proxy | `curl http://localhost:8080/health` returns 200 (health router mounts at root; nginx `location = /health` proxies exact-match). `POST http://localhost:8080/api/media/upload` returns 403 (CSRF double-submit gate) — proves `/api/media/*` router mounted via `createApp()` conditional wiring. Note: API does NOT mount a `/api/health` route; original plan text was imprecise. |
| SUMMARY frontmatter updated | SCHED-01..03, LIMIT-04..05 | Documentation grep check | `grep -A5 requirements_satisfied .planning/phases/04-*/*-SUMMARY.md` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-16
