---
phase: 11
plan: 10
type: execute
wave: 4
depends_on: [11-06]
files_modified:
  - packages/web/src/lib/headline-to-mark.ts
  - packages/web/src/lib/__tests__/headline-to-mark.test.ts
  - packages/web/src/pages/posts/PostsPage.tsx
  - packages/web/src/pages/queues/QueuePostsPage.tsx
  - packages/web/src/pages/queues/__tests__/QueuePostsPage.test.tsx
  - packages/web/src/hooks/use-posts.ts
  - packages/web/src/hooks/use-queue-posts.ts
autonomous: true
requirements: [SEARCH-01, SEARCH-02]
must_haves:
  truths:
    - "QueuePostsPage gains a search input matching PostsPage's pattern verbatim (1:1 visual parity per UI-SPEC line 51)"
    - "Both views debounce search input (~250ms) and reflect query in URL via setSearchParams({ replace: true })"
    - "Highlighted ts_headline output renders via headline-to-mark allowlist parser into React <mark> elements (NEVER dangerouslySetInnerHTML)"
    - "PostsPage usePosts call passes searchScope='posts' so server filters out queued/published"
    - "QueuePostsPage useQueuePosts call passes searchScope='queue' + queueId"
  artifacts:
    - path: "packages/web/src/lib/headline-to-mark.ts"
      provides: "renderHeadline(string) -> React.ReactNode[] mapping <b> to <mark>"
      exports: ["renderHeadline"]
    - path: "packages/web/src/pages/queues/QueuePostsPage.tsx"
      provides: "Filter bar gains the same search input"
      contains: "Search posts..."
  key_links:
    - from: "PostsPage + QueuePostsPage"
      to: "renderHeadline"
      via: "import + render in row text cell when headline present"
      pattern: "renderHeadline"
---

<objective>
Ship the client-side search surface for SEARCH-01..02. Three pieces:
1. **`headline-to-mark` allowlist parser** — converts the server's `ts_headline` HTML (`<b>...</b>` markers) into React `<mark>` elements without `dangerouslySetInnerHTML` (Pattern 2; XSS-safe by construction).
2. **QueuePostsPage search input** — exact copy of `PostsPage.tsx` lines 466-475 pattern (UI-SPEC line 51 mandates 1:1 visual parity); debounced 250ms; URL state via `setSearchParams({ replace: true })`.
3. **Render highlighted text** — both PostsPage and QueuePostsPage row text cells use `renderHeadline(post.headline ?? post.text)` when `headline` is present in the response (the server sets it only when search is active).

PostsPage already has a search input; the change there is to (a) pass `searchScope: 'posts'` to `usePosts` and (b) render `headline` when present.

Output: One new util + tests + two page modifications + one new hook (or hook extension) + one test extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-snippets-search-calendar-polish/11-SPEC.md
@.planning/phases/11-snippets-search-calendar-polish/11-CONTEXT.md
@.planning/phases/11-snippets-search-calendar-polish/11-RESEARCH.md
@.planning/phases/11-snippets-search-calendar-polish/11-PATTERNS.md
@.planning/phases/11-snippets-search-calendar-polish/11-UI-SPEC.md
@packages/web/src/pages/posts/PostsPage.tsx
@packages/web/src/pages/queues/QueuePostsPage.tsx
@packages/web/src/hooks/use-posts.ts
@packages/web/src/hooks/use-queue-posts.ts
@packages/shared/src/lib/spinnable-text.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: headline-to-mark allowlist parser + tests</name>
  <files>packages/web/src/lib/headline-to-mark.ts, packages/web/src/lib/__tests__/headline-to-mark.test.ts</files>
  <read_first>
    - packages/shared/src/lib/spinnable-text.ts (regex-driven parser analog)
    - .planning/phases/11-snippets-search-calendar-polish/11-RESEARCH.md §Pattern 2 lines 232-273 (full function body)
    - .planning/phases/11-snippets-search-calendar-polish/11-PATTERNS.md "headline-to-mark.ts" row
  </read_first>
  <behavior>
    - Test 1: empty string returns empty array
    - Test 2: `'plain text no markers'` returns `['plain text no markers']`
    - Test 3: `'<b>match</b> rest'` returns `[<mark>match</mark>, ' rest']` (a single React.ReactNode array)
    - Test 4: `'a <b>b</b> c <b>d</b> e'` returns `['a ', <mark>b</mark>, ' c ', <mark>d</mark>, ' e']`
    - Test 5: HTML entities `&amp;`, `&lt;`, `&gt;`, `&quot;`, `&#39;` are decoded in non-marked segments (ts_headline emits these)
    - Test 6: malformed input `'<b>unclosed'` (missing `</b>`) — function does not throw; renders the rest as marked text (acceptable since ts_headline always closes its markers, so this is a defensive case)
    - Test 7: input with `<script>` tag — the function never emits the script tag through React (treats it as plain text via the split, which then renders as a string child — React escapes it). Asserts that `renderHeadline('<script>alert(1)</script>')` produces text content `'<script>alert(1)</script>'` rendered as escaped text — never injected DOM. (The test renders the output and queries for the escaped text.)
  </behavior>
  <action>
    Create `packages/web/src/lib/headline-to-mark.ts`. Copy the body verbatim from RESEARCH §Pattern 2 lines 256-273:

        import * as React from 'react';

        export function renderHeadline(headline: string): React.ReactNode[] {
          // ts_headline produces only <b>...</b> markers; everything else is HTML-escaped.
          const parts = headline.split(/(<b>|<\/b>)/g);
          let inMark = false;
          const out: React.ReactNode[] = [];
          parts.forEach((part, i) => {
            if (part === '<b>') { inMark = true; return; }
            if (part === '</b>') { inMark = false; return; }
            if (!part) return;
            const decoded = part
              .replace(/&amp;/g, '&')
              .replace(/&lt;/g, '<')
              .replace(/&gt;/g, '>')
              .replace(/&quot;/g, '"')
              .replace(/&#39;/g, "'");
            out.push(inMark ? <mark key={i}>{decoded}</mark> : decoded);
          });
          return out;
        }

    Per UI-SPEC §Color line 52: the `<mark>` element is styled by global CSS at `bg-warning/30 text-foreground rounded-sm px-1`. Apply this className via `<mark className="bg-warning/30 text-foreground rounded-sm px-1" key={i}>` so consumers do not need to wrap.

    Create `packages/web/src/lib/__tests__/headline-to-mark.test.ts` covering the 7 cases. Use `@testing-library/react` `render` to verify Test 7 produces escaped text (never an actual `<script>` element in the DOM).
  </action>
  <verify>
    <automated>pnpm --filter @sms/web test -- --run headline-to-mark.test</automated>
  </verify>
  <done>
    - All 7 tests pass
    - `grep -c "dangerouslySetInnerHTML" packages/web/src/lib/headline-to-mark.ts` returns 0
    - Test 7 explicitly asserts that script content renders as escaped text, not as an injected DOM node
  </done>
</task>

<task type="auto">
  <name>Task 2: PostsPage searchScope + render headline; QueuePostsPage gain search input + use renderHeadline</name>
  <files>packages/web/src/pages/posts/PostsPage.tsx, packages/web/src/pages/queues/QueuePostsPage.tsx, packages/web/src/pages/queues/__tests__/QueuePostsPage.test.tsx, packages/web/src/hooks/use-posts.ts, packages/web/src/hooks/use-queue-posts.ts</files>
  <read_first>
    - packages/web/src/pages/posts/PostsPage.tsx (full file — search input lines ~466-475; URL state via setSearchParams; debounce ~lines 124, 162-167)
    - packages/web/src/pages/queues/QueuePostsPage.tsx (current shape — find filter bar / table)
    - packages/web/src/hooks/use-posts.ts (existing usePosts; add searchScope param)
    - packages/web/src/hooks/use-queue-posts.ts (existing useQueuePosts; add searchScope + queueId already present)
    - .planning/phases/11-snippets-search-calendar-polish/11-UI-SPEC.md line 51 (1:1 visual parity required across PostsPage / QueuePostsPage / CalendarPage search inputs) + Copywriting (placeholder `Search posts...`, aria-label `Search posts`)
    - .planning/phases/11-snippets-search-calendar-polish/11-PATTERNS.md "QueuePostsPage" search row
    - .planning/phases/11-snippets-search-calendar-polish/11-CONTEXT.md Claude's-Discretion (250ms debounce, replace:true)
  </read_first>
  <action>
    1. Modify `packages/web/src/hooks/use-posts.ts`:
       - Extend the `usePosts(filters)` hook so `filters` accepts an optional `searchScope?: 'posts' | 'queue' | 'calendar'` field. Append `&searchScope=...` to the URLSearchParams when set.
       - Update the response type to include the optional `headline?: string` and `rank?: number` fields per plan 01's posts schema extension.

    2. Modify `packages/web/src/hooks/use-queue-posts.ts` similarly: add `searchScope` parameter (always `'queue'` from QueuePostsPage; the hook can either accept it or hardcode it — match the existing convention).

    3. Modify `packages/web/src/pages/posts/PostsPage.tsx`:
       - In the `usePosts(...)` call, set `searchScope: 'posts'` (always) so the backend excludes queued/published. Existing search input wiring is preserved.
       - In the row's text cell, when `post.headline` is defined, render `{renderHeadline(post.headline)}` (import from `../../lib/headline-to-mark`); when undefined, render `post.text` as today.
       - **Do NOT** change the existing 300ms debounce timing if it is already in PostsPage — UI-SPEC §Interaction allows either 250 or 300ms; do not introduce drift in this plan.

    4. Modify `packages/web/src/pages/queues/QueuePostsPage.tsx`:
       - Add the search input per UI-SPEC line 51. Copy the JSX from PostsPage.tsx lines 466-475 verbatim:
             <div className="relative flex-1 min-w-[200px]">
               <Search className="absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" />
               <Input
                 placeholder="Search posts..."
                 value={searchInput}
                 onChange={(e) => setSearchInput(e.target.value)}
                 className="pl-9"
                 aria-label="Search posts"
               />
             </div>
         Mount it inside the existing filter bar / page header. Add `useState` for `searchInput` and a `useEffect` that debounces 250ms (Claude's-Discretion default; see CONTEXT.md) into a `setSearchParams(...)` call with `{ replace: true }`. Pattern to copy: PostsPage lines 162-167 (debounce) + the URL state idiom used elsewhere in QueuePostsPage.
       - Wire `useQueuePosts({ ..., search: searchInput.trim() || undefined, searchScope: 'queue', queueId })` so the search query parameter reaches the backend.
       - Like PostsPage, render `renderHeadline(post.headline)` in the text cell when `post.headline` is present.

    5. Extend `packages/web/src/pages/queues/__tests__/QueuePostsPage.test.tsx` with two tests:
       - **Test A — search input present and accessible:**
         Render the page. Assert `screen.getByLabelText('Search posts')` exists.
       - **Test B — typing into search updates URL after debounce:**
         Render the page. Type `'announcement'` into the search input. Advance fake timers by 250ms. Assert `setSearchParams` was called with an object containing `search: 'announcement'` and `{ replace: true }`. (Use `vi.useFakeTimers()` per CLAUDE.md §Testing.)
       - **Test C — clearing the input removes the URL param:**
         As above, then clear the input, advance timers. Assert the search param is removed from the URL.

    6. Confirm visual parity by reading the final Posts/Queue/Calendar JSX side-by-side. The three search inputs must use the same className strings (`relative flex-1 min-w-[200px]` outer, `pl-9` on the Input). The CalendarPage search input is added in plan 11; visual parity is checker-enforced there.
  </action>
  <verify>
    <automated>pnpm --filter @sms/web test -- --run QueuePostsPage.test PostsPage.test headline-to-mark.test && pnpm --filter @sms/web exec tsc --noEmit</automated>
  </verify>
  <done>
    - QueuePostsPage has a search input with placeholder `Search posts...` and aria-label `Search posts`
    - Both PostsPage and QueuePostsPage render `renderHeadline(post.headline)` when present
    - `usePosts` and `useQueuePosts` send `searchScope` to the backend
    - QueuePostsPage Test B asserts debounced URL update at 250ms
    - tsc --noEmit clean
    - `grep -c 'placeholder="Search posts\\.\\.\\."' packages/web/src/pages/queues/QueuePostsPage.tsx` returns 1
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server `ts_headline` HTML → client React | Server emits HTML-escaped text with `<b>` markers; client parser MUST NOT eval / inject as DOM |
| URL search param → backend | Already validated server-side (plan 06); client just plumbs through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10-01 | Tampering (XSS via ts_headline output) | renderHeadline | mitigate | Allowlist parser splits on `<b>` / `</b>` only; everything else is treated as text and rendered via React's automatic escaping. Never `dangerouslySetInnerHTML`. Test 7 explicitly verifies a `<script>` payload renders as text |
| T-11-10-02 | Tampering (XSS if Postgres ts_headline is misconfigured) | StartSel/StopSel config | mitigate | Server passes `'StartSel=<b>, StopSel=</b>'` literally (plan 06); the client parser only recognizes those two markers. If a future change broadens the allowed tags, the parser silently degrades to plain text — never opens an injection path |
| T-11-10-03 | DoS (huge headline string) | renderHeadline | accept | ts_headline output bounded by `MaxWords=20`; defensive split is O(n); not a real attack surface |
| T-11-10-04 | UX (search input drops state on URL push) | setSearchParams replace:true | mitigate | `{ replace: true }` per CONTEXT Claude's-Discretion; back button skips keystroke history |
</threat_model>

<verification>
- `pnpm --filter @sms/web test -- --run` exits 0 for all touched test files
- tsc --noEmit clean
- No `dangerouslySetInnerHTML` introduced anywhere in this plan
- Search input copy is identical across all views (UI-SPEC line 51 enforcement)
</verification>

<success_criteria>
- User can search Scheduled Posts (PostsPage) and Queue Posts (QueuePostsPage); results show ranked highlights
- Highlighted matching tokens render as `<mark>` with the warning-tinted background
- A future XSS in `ts_headline` output is structurally impossible — verified by Test 7
</success_criteria>

<output>
After completion, create `.planning/phases/11-snippets-search-calendar-polish/11-10-SUMMARY.md`
</output>
