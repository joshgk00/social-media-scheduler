---
phase: 06.5
slug: bull-board-nginx-proxy
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-22
---

# Phase 06.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.x (existing) |
| **Config file** | `packages/api/vitest.config.ts` |
| **Quick run command** | `pnpm --filter @sms/api test` |
| **Full suite command** | `pnpm -r test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @sms/api test`
- **After every plan wave:** Run `pnpm -r test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 6.5-01-01 | 01 | 1 | MEDIA-06 | — | N/A | manual | `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/media/nonexistent.jpg` returns 404 (not 200 HTML) | N/A — manual | pending |
| 6.5-01-02 | 01 | 1 | MEDIA-01 | — | N/A | manual | Upload image via API, then `curl http://localhost:8080/media/{thumbnailKey} \| file -` shows image type | N/A — manual | pending |
| 6.5-01-03 | 01 | 1 | FLOW-E | — | N/A | manual | `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/admin/queues` returns 401 | N/A — manual | pending |

*Status: pending / green / red / flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.*

No new test infrastructure needed. This phase's verification is manual Docker-based curl commands, not automated test files.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| `/media/{key}` returns actual file through nginx port | MEDIA-06 | Requires running Docker Compose stack; tests nginx config behavior, not application code | 1. `docker compose up --build -d` 2. Upload image via API 3. `curl -s http://localhost:8080/media/{key} \| file -` must show image type, not HTML |
| Thumbnail URL returns image through nginx port | MEDIA-01 | Requires running Docker Compose stack; tests nginx proxy pass behavior | 1. Upload image to create thumbnail 2. `curl http://localhost:8080/media/{thumbnailKey} \| file -` must show image type |
| `/admin/queues` returns 401 (auth gate) not 200 (SPA HTML) through nginx port | FLOW-E | Requires running Docker Compose stack; tests nginx proxy vs SPA catch-all | `curl -s -o /dev/null -w "%{http_code}" http://localhost:8080/admin/queues` must return 401 |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
