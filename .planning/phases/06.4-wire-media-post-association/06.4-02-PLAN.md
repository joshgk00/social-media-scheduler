---
phase: 06.4-wire-media-post-association
plan: 02
type: execute
wave: 2
depends_on:
  - 06.4-01
files_modified:
  - packages/api/src/__tests__/integration/posts-api.test.ts
autonomous: true
requirements:
  - MEDIA-05
user_setup: []

must_haves:
  truths:
    - "POST /api/posts with mediaIds passes mediaIds through to createPost service"
    - "PATCH /api/posts/:id with mediaIds passes mediaIds through to updatePost service"
    - "Full API test suite passes including new integration tests"
  artifacts:
    - path: "packages/api/src/__tests__/integration/posts-api.test.ts"
      provides: "Integration tests for POST/PATCH routes with mediaIds"
      contains: "mediaIds"
  key_links:
    - from: "packages/api/src/routes/posts.ts"
      to: "packages/api/src/services/post.service.ts"
      via: "createPost(db, userId, parsed.data) where parsed.data includes mediaIds"
      pattern: "createPost.*parsed\\.data"
---

<objective>
Add integration tests that verify POST/PATCH `/api/posts` routes pass `mediaIds` through to the service layer, and run full suite verification.

Purpose: Confirm the Zod-validated `mediaIds` flows from HTTP request body through the route handler to the service function. Validates MEDIA-05 end-to-end at the API layer.

Output: Integration tests in `posts-api.test.ts` + full suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.4-wire-media-post-association/06.4-RESEARCH.md
@.planning/phases/06.4-wire-media-post-association/06.4-PATTERNS.md
@.planning/phases/06.4-wire-media-post-association/06.4-01-SUMMARY.md

<interfaces>
<!-- From posts-api.test.ts existing mock pattern (lines 7-25): -->
```typescript
const mockCreatePost = vi.fn();
const mockUpdatePost = vi.fn();

vi.mock('../../services/post.service.js', async () => {
  const { PostServiceError } = await vi.importActual<...>('...');
  return {
    createPost: (...args: unknown[]) => mockCreatePost(...args),
    updatePost: (...args: unknown[]) => mockUpdatePost(...args),
    // ...
  };
});
```

<!-- Routes pass parsed.data directly (line 160, 316): -->
```typescript
const post = await createPost(db, userId, parsed.data);
const updatedPost = await updatePost(db, userId, postId, parsed.data);
```

<!-- Zod schemas already include mediaIds: -->
```typescript
// packages/shared/src/schemas/posts.ts
mediaIds: z.array(z.string().uuid()).optional(),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add integration tests for POST/PATCH with mediaIds</name>
  <files>packages/api/src/__tests__/integration/posts-api.test.ts</files>
  <read_first>
    - packages/api/src/__tests__/integration/posts-api.test.ts (full file -- understand existing test patterns, mock setup, app creation, auth simulation)
    - packages/api/src/routes/posts.ts (confirm parsed.data flow)
  </read_first>
  <action>
Add integration tests to `packages/api/src/__tests__/integration/posts-api.test.ts` that verify mediaIds flows through the route to the service layer.

The tests use `supertest` with `createApp` and mock all service functions. The existing `mockCreatePost` and `mockUpdatePost` are already captured via `vi.fn()`. The tests verify what arguments the mocks receive.

Add a new `describe('media association', ...)` block (or add to existing POST/PATCH describe blocks) with these tests:

**Test 1: POST /api/posts passes mediaIds to createPost**
- Send `POST /api/posts` with body: `{ profileId: 'profile-uuid', text: 'Hello', mediaIds: ['media-uuid-1', 'media-uuid-2'] }`
  (use valid UUIDs like `'00000000-0000-0000-0000-000000000001'` if the Zod schema requires UUID format)
- Mock `mockCreatePost` to resolve with a valid post object
- Assert `mockCreatePost` was called with `expect.objectContaining({ mediaIds: ['media-uuid-1', 'media-uuid-2'] })` as the third argument

**Test 2: POST /api/posts without mediaIds passes undefined mediaIds**
- Send `POST /api/posts` with body: `{ profileId: 'profile-uuid', text: 'Hello' }` (no mediaIds)
- Assert `mockCreatePost` third arg does NOT contain `mediaIds` key, OR that `mediaIds` is `undefined`

**Test 3: PATCH /api/posts/:id passes mediaIds to updatePost**
- Send `PATCH /api/posts/some-post-id` with body: `{ postVersion: 1, mediaIds: ['media-uuid-3'] }`
- Mock `mockUpdatePost` to resolve with a valid post object
- Assert `mockUpdatePost` was called with `expect.objectContaining({ mediaIds: ['media-uuid-3'] })` as the fourth argument

**Test 4: PATCH /api/posts/:id with empty mediaIds passes empty array**
- Send `PATCH /api/posts/some-post-id` with body: `{ postVersion: 1, mediaIds: [] }`
- Assert `mockUpdatePost` fourth arg contains `{ mediaIds: [] }`

Follow existing test patterns for:
- Auth simulation (how existing tests set up session/userId)
- Rate limit mock setup
- The `app` creation via `createApp`
- Response status assertions
- `beforeEach` cleanup of mocks
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler/packages/api && pnpm vitest run src/__tests__/integration/posts-api.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `posts-api.test.ts` contains test with text "passes mediaIds to createPost" or similar
    - `posts-api.test.ts` contains test with text "passes mediaIds to updatePost" or similar
    - `posts-api.test.ts` contains assertion `expect.objectContaining({ mediaIds:` pattern
    - All tests in `posts-api.test.ts` pass (exit code 0)
  </acceptance_criteria>
  <done>Four integration tests pass verifying mediaIds flows from HTTP request body through Zod validation and route handler to service function for both POST and PATCH. All existing integration tests remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite verification</name>
  <files></files>
  <read_first>
    - packages/api/src/__tests__/services/post.test.ts (verify Task 1 from Plan 01 still passes)
  </read_first>
  <action>
Run the full test suite across both API and worker packages to verify nothing is broken:

1. `pnpm --filter @sms/api test` -- all API tests including unit + integration
2. `pnpm --filter @sms/worker test` -- worker tests including the existing media-readiness gate test

If any test fails, investigate and fix. The worker tests should pass without changes -- the media-readiness gate in `post-lifecycle.service.ts` was already correctly implemented and just needed the FK to be set (which Plan 01 wired up).

Also verify TypeScript compiles across all packages:
`pnpm --filter @sms/api --filter @sms/worker exec tsc --noEmit`
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && pnpm --filter @sms/api --filter @sms/worker test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @sms/api test` exits with code 0
    - `pnpm --filter @sms/worker test` exits with code 0
    - No TypeScript compilation errors in api or worker packages
  </acceptance_criteria>
  <done>Full test suite green across API (unit + integration) and worker packages. TypeScript compiles clean. MEDIA-05 wiring is complete and verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | mediaIds array validated by Zod at route entry (UUID format, optional) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6.4-05 | Tampering | routes/posts.ts mediaIds | mitigate | Zod `z.array(z.string().uuid()).optional()` rejects non-UUID mediaIds at route entry before reaching service layer. Verified by integration tests. |
</threat_model>

<verification>
1. Integration tests pass: `cd packages/api && pnpm vitest run src/__tests__/integration/posts-api.test.ts --reporter=verbose` exits 0
2. Full API suite: `pnpm --filter @sms/api test` exits 0
3. Full worker suite: `pnpm --filter @sms/worker test` exits 0
4. TypeScript: `pnpm --filter @sms/api --filter @sms/worker exec tsc --noEmit` exits 0
</verification>

<success_criteria>
- Integration tests verify mediaIds passes from HTTP body through to service functions
- All tests green across API and worker packages
- MEDIA-05 is fully wired: frontend sends mediaIds -> Zod validates -> route passes to service -> service associates inside transaction -> publish worker gate fires correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06.4-wire-media-post-association/06.4-02-SUMMARY.md`
</output>
