---
phase: 01-infrastructure-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 03
  - 04
files_modified:
  - packages/db/drizzle/meta/_journal.json
  - packages/db/drizzle/0000_empty_baseline.sql
autonomous: false
requirements:
  - INFRA-04
  - INFRA-01

must_haves:
  truths:
    - "Drizzle migration infrastructure produces a baseline migration file"
    - "pnpm -r build compiles all packages without errors"
    - "pnpm -r test -- --run passes all tests across all packages"
    - "docker compose config validates both compose files"
  artifacts:
    - path: "packages/db/drizzle/meta/_journal.json"
      provides: "Drizzle migration journal tracking applied migrations"
    - path: "packages/db/drizzle/0000_empty_baseline.sql"
      provides: "Baseline empty migration (D-07)"
  key_links:
    - from: "packages/api/src/index.ts"
      to: "packages/db/src/migrate.ts"
      via: "import runMigrations"
      pattern: "runMigrations"
    - from: "packages/db/src/migrate.ts"
      to: "packages/db/drizzle/"
      via: "migrationsFolder"
      pattern: "migrationsFolder.*drizzle"
---

<objective>
Generate the baseline empty Drizzle migration (D-07), verify the full build pipeline compiles all packages, run the complete test suite, and validate Docker Compose configuration end-to-end. This is the integration verification plan that confirms all Phase 1 pieces fit together.

Purpose: The individual plans created packages, middleware, encryption, Docker config, and health endpoints. This plan ensures they compose correctly: TypeScript compiles across workspace boundaries, tests pass in every package, and the migration infrastructure works. The schema push step verifies migration tooling is functional.

Output: Baseline migration file, confirmed build + test pipeline, ready-to-deploy infrastructure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation/01-CONTEXT.md

@.planning/phases/01-infrastructure-foundation/01-01-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Generate baseline migration and verify build pipeline</name>
  <files>
    packages/db/drizzle/meta/_journal.json,
    packages/db/drizzle/0000_empty_baseline.sql
  </files>
  <read_first>
    packages/db/drizzle.config.ts (Drizzle config from Plan 01)
    packages/db/src/schema/index.ts (empty schema from Plan 01)
    packages/db/src/migrate.ts (migration runner from Plan 01)
    packages/api/src/index.ts (entry point from Plan 04 -- confirm it imports runMigrations)
    packages/shared/src/index.ts (confirm encryption re-exports from Plan 03)
  </read_first>
  <action>
**Step 1: Full build verification**

Run TypeScript compilation across all packages:
```bash
cd /Users/slaughterassistant/social-media-scheduler
pnpm -r build
```

Fix any compilation errors. Common issues:
- Missing type imports across workspace boundaries
- `workspace:*` references not resolving (run `pnpm install` if needed)
- Missing `"type": "module"` in package.json files

**Step 2: Full test suite**

```bash
pnpm -r test -- --run
```

This should run:
- `@sms/shared` tests (encryption round-trip from Plan 03)
- `@sms/api` tests (health endpoint + middleware from Plan 04)
- `@sms/worker` tests (heartbeat from Plan 04)

Fix any test failures.

**Step 3: Generate baseline migration (D-07)**

Per D-07, Phase 1 creates migration infrastructure only with a baseline empty migration. Since the schema is empty (no tables), generate an initial migration:

```bash
# Create the drizzle migrations directory if it doesn't exist
mkdir -p packages/db/drizzle

# Generate migration from empty schema
# This creates the migration journal and a baseline SQL file
cd packages/db
npx drizzle-kit generate 2>&1 || true
cd ../..
```

If `drizzle-kit generate` produces no output because the schema is empty (no tables defined), manually create the baseline:

1. Create `packages/db/drizzle/meta/_journal.json`:
```json
{
  "version": "7",
  "dialect": "postgresql",
  "entries": []
}
```

2. The `drizzle/` directory existing with the journal file is sufficient for the migration runner to work (it will find zero migrations to apply, which is correct for Phase 1).

**Step 4: Docker Compose validation**

```bash
docker compose config --quiet
docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet
```

Both must exit 0.

**Step 5: Verify workspace cross-references work**

```bash
# Verify api can import from db and shared
node -e "
  const { createDbClient } = await import('./packages/db/dist/client.js');
  const { encrypt } = await import('./packages/shared/dist/encryption.js');
  console.log('Cross-package imports OK');
  console.log('createDbClient:', typeof createDbClient);
  console.log('encrypt:', typeof encrypt);
" 2>&1 || echo "Cross-package import check -- may need build first"
```
  </action>
  <verify>
    <automated>cd /Users/slaughterassistant/social-media-scheduler && pnpm -r build 2>&1 && echo "---BUILD OK---" && pnpm -r test -- --run 2>&1 && echo "---TESTS OK---" && test -d packages/db/drizzle && echo "drizzle dir exists" && docker compose config --quiet 2>&1 && echo "---COMPOSE VALID---"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm -r build` exits 0 (all packages compile)
    - `pnpm -r test -- --run` exits 0 (all tests pass across all packages)
    - `packages/db/drizzle/` directory exists
    - `packages/db/drizzle/meta/_journal.json` exists with valid JSON
    - `docker compose config --quiet` exits 0
    - `docker compose -f docker-compose.yml -f docker-compose.dev.yml config --quiet` exits 0
    - `packages/api/src/index.ts` contains `runMigrations` import from `@sms/db`
    - `packages/shared/src/index.ts` contains `export` for `encrypt` and `decrypt`
  </acceptance_criteria>
  <done>Full build pipeline works. All tests pass. Baseline migration infrastructure created. Docker Compose validates. Workspace cross-references resolve.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 1 infrastructure</name>
  <files></files>
  <what-built>
Complete Phase 1 infrastructure:
- pnpm monorepo with 5 packages (shared, db, api, worker, web)
- Docker Compose stack with 5 services (api, worker, postgres, redis, nginx)
- Multi-stage Dockerfile with native addon support and ffmpeg
- AES-256-GCM encryption module with TDD tests
- Express API with health endpoint, CSRF protection, security headers, structured logging, correlation IDs
- Worker with Redis heartbeat (30s interval)
- Drizzle ORM migration infrastructure with baseline
- nginx reverse proxy (plain HTTP, Cloudflare Tunnel handles TLS)
  </what-built>
  <action>Human reviews and verifies the complete Phase 1 infrastructure.</action>
  <how-to-verify>
1. Check build: `cd /Users/slaughterassistant/social-media-scheduler && pnpm -r build` -- should exit 0
2. Check tests: `pnpm -r test -- --run` -- all tests should pass
3. Check workspace: `pnpm ls -r --depth 0` -- should list all 5 packages
4. Check Docker: `docker compose config --quiet` -- should exit 0
5. Verify `.env.example` has all required vars documented
6. Spot-check encryption: `node -e "const { encrypt, decrypt, validateEncryptionKey } = await import('./packages/shared/dist/index.js'); const key = validateEncryptionKey(require('crypto').randomBytes(32).toString('hex')); const enc = encrypt('test', key); console.log('Encrypted:', enc); console.log('Decrypted:', decrypt(enc.ciphertext, enc.iv, enc.authTag, key));"` -- should show encrypted payload and "test"
7. Review nginx/nginx.conf -- should proxy /api/ to api:3000 and / to web:5173, no TLS/SSL
  </how-to-verify>
  <verify>Human approval</verify>
  <done>Human has verified Phase 1 infrastructure is correct and complete.</done>
  <resume-signal>Type "approved" to complete Phase 1, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build pipeline -> production image | Compiled code must match source; multi-stage build ensures build tools don't leak to production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-13 | Tampering | Migration files | accept | Baseline migration is empty. Future migrations will be generated by drizzle-kit from schema definitions. Single-developer project with version control. |
| T-1-14 | Information Disclosure | Build artifacts | mitigate | `.dockerignore` excludes `.env`, `.planning`, `.git`, `node_modules` from Docker build context. Production image contains only compiled JS and runtime deps. |
</threat_model>

<verification>
- `pnpm -r build` exits 0
- `pnpm -r test -- --run` exits 0
- `packages/db/drizzle/` directory exists with journal
- `docker compose config --quiet` exits 0
- Human verification confirms infrastructure is correct
</verification>

<success_criteria>
Phase 1 is fully integrated: all packages build, all tests pass, Docker infrastructure validates, migration tooling works, and human has verified the complete infrastructure. Ready for Phase 2 (Authentication & User Account).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-05-SUMMARY.md`
</output>
