---
phase: 06.2
slug: test-build-stabilization
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-16
---

# Phase 06.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest 4.1.3 |
| **Config file** | `packages/db/vitest.config.ts` (exists; extended in Wave 0 to add coverage) |
| **Quick run command** | `pnpm --filter @sms/db test -- --run` |
| **Full suite command** | `pnpm test` (root — runs all packages) |
| **Estimated runtime** | ~30s quick / ~2–4m full |

Test database: reused dev docker-compose `postgres` container on `127.0.0.1:5432`. Tests connect via `DATABASE_URL_TEST` env var (drops + recreates `public` schema between suites — see Wave 0 `test-db.ts` helper).

---

## Sampling Rate

- **After every task commit:** `pnpm --filter @sms/db test -- --run` (quick; ~30s)
- **After every plan wave:** `pnpm test` (full root suite)
- **Before `/gsd-verify-work`:** Full suite green + `pnpm --filter @sms/db test -- --run --coverage` reports 100% branches for `packages/db/src/migrate.ts`
- **Max feedback latency:** ~30s

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 06.2-01-01 | 01 | 1 | SC1 — dist current | — | N/A | build | `pnpm build` | ✓ | ✅ green |
| 06.2-01-02 | 01 | 1 | SC1 — pretest guardrail | — | N/A | smoke | `pnpm test` triggers `pnpm -r build` first | ✓ (root `package.json` pretest) | ✅ green |
| 06.2-01-03 | 01 | 1 | SC2 — mock-db .returning() | — | Mock mirrors real postgres-js row shape | unit | `pnpm --filter @sms/api test -- --run` (291 tests pass) | ✓ | ✅ green |
| 06.2-02-01 | 02 | 2 | SC4 — advisory lock acquire | T-06.2-01 (concurrent-migrator race) | `sql.reserve()`-pinned connection holds `pg_advisory_lock` for duration of run | unit/integration | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "advisory lock"` | ✓ (`migrate.test.ts` #6) | ✅ green |
| 06.2-02-02 | 02 | 2 | SC5 — per-migration transaction | T-06.2-02 (partial-migration crash loop) | Non-duplicate error aborts tx; `__drizzle_migrations` unchanged | unit | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "rollback"` | ✓ (`migrate.test.ts` #5) | ✅ green |
| 06.2-02-03 | 02 | 2 | SC5 — duplicate-object tolerance | T-06.2-03 (drift masking) | Narrowed codes (`42P07`, `42P06`, `42710`, `42701`) swallowed at statement level; others rethrow | unit | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "duplicate"` | ✓ (`migrate.test.ts` #3+#4) | ✅ green |
| 06.2-03-01 | 03 | 2 | SC6 — fresh DB apply | — | N/A | unit | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "fresh"` | ✓ (`migrate.test.ts` #1) | ✅ green |
| 06.2-03-02 | 03 | 2 | SC6 — idempotent re-run | — | N/A | unit | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "idempotent"` | ✓ (`migrate.test.ts` #2) | ✅ green |
| 06.2-03-03 | 03 | 2 | SC6 — orphan-schema baseline | — | N/A | unit | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "orphan"` | ✓ (`migrate.test.ts` #3) | ✅ green |
| 06.2-03-04 | 03 | 2 | SC6 — concurrent-caller lock | T-06.2-01 | Shared `order[]` array proves serialization; journal has 4 rows not 8 | integration | `pnpm --filter @sms/db test -- --run migrate.test.ts -t "concurrent"` | ✓ (`migrate.test.ts` #6) | ✅ green |
| 06.2-03-05 | 03 | 3 | SC6 — 100% branch coverage | — | N/A | coverage | `pnpm --filter @sms/db test -- --run --coverage` (thresholds 100 branches for `migrate.ts`) | ✓ (vitest.config.ts coverage) | ✅ green |
| 06.2-verify | — | 3 | SC3 — all packages green | — | N/A | e2e | `pnpm test` exits 0 | ✓ | ✅ green |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] `packages/db/src/__tests__/migrate.test.ts` — new test file covering SC4, SC5, SC6 (6 scenarios)
- [x] `packages/db/src/__tests__/helpers/test-db.ts` — `createTestClient()` + `resetSchemas()` + shared `DATABASE_URL_TEST` fixture
- [x] `packages/db/vitest.config.ts` — extend with `test.coverage` (v8 provider, `include: ['src/migrate.ts']`, `thresholds.branches: 100`)
- [x] Root `package.json` — add `pretest: "pnpm -r build"` script
- [x] `.env.example` — add `DATABASE_URL_TEST=postgres://scheduler:phase2password!@127.0.0.1:5432/scheduler_test` (points at dev compose)
- [x] `packages/db/package.json` — no new prod deps; add `@vitest/coverage-v8` to devDependencies if absent

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Cold-boot on fresh Postgres instance (clean slate migration application end-to-end) | SC6 (integration confidence) | Covered by automated "fresh DB apply" test but worth one manual smoke to confirm under real container boot | `docker compose down -v && docker compose up -d postgres && pnpm --filter @sms/api dev` — tail logs, confirm migrations log lines match test expectation |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references
- [x] No watch-mode flags
- [x] Feedback latency < 30s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved (revision pass 2026-04-16 — reconciled `DATABASE_URL_TEST` value and service name with Plans 01/03; flipped sign-off boxes after confirming plans structurally satisfy Nyquist)

---

## Validation Audit 2026-04-16

| Metric | Count |
|--------|-------|
| Gaps found | 0 |
| Resolved | 0 |
| Escalated | 0 |

**Audit method:** Post-execution cross-reference of all 12 verification map entries against live codebase and test runs.

**Evidence collected:**
- `pnpm build` — exits 0 (all 5 packages)
- `pnpm --filter @sms/db test -- --run` — 6 passed (6), 905ms
- `pnpm --filter @sms/db test -- --run --coverage` — branches 100%, statements 95.23%, lines 95.16%
- `pnpm --filter @sms/api test -- --run` — 291 passed, 13 todo, 0 failures
- `pnpm -r test -- --run` — shared 126/126, db 6/6, api 291/291, worker 94/94 — all green
- `migrate.test.ts` contains 6 `it()` blocks covering all D-08 scenarios
- Root `package.json` pretest script confirmed: `pnpm -r build`
- `vitest.config.ts` coverage thresholds confirmed: branches 100

**Verdict:** All requirements have automated verification. No auditor agent needed.
