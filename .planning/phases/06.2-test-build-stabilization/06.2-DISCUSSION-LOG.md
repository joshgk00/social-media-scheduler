# Phase 6.2: Test & Build Stabilization + Migration Runner Hardening - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-16
**Phase:** 06.2-test-build-stabilization
**Areas discussed:** Review follow-up scope

---

## Gray Area Selection

The user was presented with four candidate gray areas. Only one was selected for discussion; the others were resolved via Claude's Discretion using defaults captured in CONTEXT.md.

| Gray Area | Description | Selected for Discussion |
|-----------|-------------|-------------------------|
| Advisory lock semantics | `pg_advisory_lock` (wait) vs `pg_try_advisory_lock` (fail fast) | |
| Migration test strategy | Real Postgres via testcontainers vs stubbed client | |
| `mock-db` `.returning()` shape | Echo SET payload vs fixture-configured rows vs per-call stub | |
| Review follow-up scope | Which REVIEW.md items (M-02/M-03/I-03/M-04/L-01/L-02) fold into 6.2 | ✓ |

**Defaults applied to unselected areas:**
- Advisory lock → `pg_advisory_lock` (blocking wait), matching REVIEW.md fix snippet
- Migration tests → real Postgres instance (stub can't emulate `client.begin`/advisory locks faithfully); specific testcontainer choice left to planner
- `mock-db.returning()` → returns `[{ ...matchedRow, ...setPayload }]`

---

## Review Follow-Up Scope

### M-02: Narrow DUPLICATE_OBJECT_CODES to migration-relevant SQLSTATE codes

| Option | Description | Selected |
|--------|-------------|----------|
| Fold in | Drop `42P04`, `42P03`, `42P05`; keep `42P07`, `42P06`, `42710`, `42701`. Trivial, co-located with migrate.ts rewrite. | ✓ |
| Defer to backlog | Cosmetic hardening; not required for production safety. | |

**User's choice:** Fold in (recommended)
**Notes:** Lands in the same commit as H-01/H-02 so migrate.ts rewrite is one atomic change.

---

### M-03: Brittle `--> statement-breakpoint` delimiter coupling

| Option | Description | Selected |
|--------|-------------|----------|
| Add delimiter test only | One test in migrate.test.ts pins the delimiter; catches silent drizzle bumps. | |
| Defer entirely | Drizzle version pinned via tilde; format hasn't changed in years. Low probability of silent break. | ✓ |
| Fold full fix (pin + runtime check) | More thorough; expands 6.2 scope into dependency hardening. | |

**User's choice:** Defer entirely (recommended)
**Notes:** Revisit only if CI break surfaces. Tracked in CONTEXT.md deferred ideas.

---

### I-03: Migration runner uses console.log/console.warn instead of pino

| Option | Description | Selected |
|--------|-------------|----------|
| Defer to backlog | Info-level observability gap; not blocking. 6.2 is test+build + migration safety. | ✓ |
| Fold in | Thread pino child logger through `runMigrations()`; pulls pino into `@sms/db`. | |

**User's choice:** Defer to backlog (recommended)
**Notes:** Belongs in a broader logging consolidation phase.

---

### M-04 / L-01 / L-02: Pre-existing low-impact items

| Option | Description | Selected |
|--------|-------------|----------|
| Defer all | None blocking; fixing in 6.2 expands surface area without tightening success criteria. | ✓ |
| Fold L-01 only (PORT validation) | Two lines in packages/api/src/index.ts. | |
| Fold all three | Full sweep across nginx + env validation + connection pooling. | |

**User's choice:** Defer all (recommended)

---

## Claude's Discretion

Per user selection, the following were not discussed interactively and are resolved using defaults in CONTEXT.md:

- Advisory lock wait vs try — wait (default)
- Migration test Postgres source — planner picks testcontainers, reused docker-compose, or embedded
- `mock-db.returning()` return shape — merged SET-over-match rows
- Stale-dist build guardrail mechanism — planner picks pretest script, CI mtime check, or delete-dist
- Whether `mock-db` `.returning()` extends beyond `updateChain` — add on first failing test

## Deferred Ideas

- M-03 (delimiter coupling)
- M-04 (migration client shares DATABASE_URL with app pool)
- I-03 (console.log in migration runner)
- L-01 (PORT parseInt validation)
- L-02 (nginx query-string logging)
