---
phase: 04
plan: 6
plan_number: 6
type: execute
wave: 5
depends_on: [3, 4, 5]
files_modified:
  - packages/worker/src/__tests__/integration/post-lifecycle.integration.test.ts
  - packages/worker/src/__tests__/integration/shutdown.integration.test.ts
  - packages/worker/src/__tests__/integration/failed-listener.integration.test.ts
  - packages/worker/src/__tests__/helpers/testcontainer.ts
  - packages/worker/vitest.config.ts
  - .planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md
autonomous: false
requirements:
  - WORKER-01
  - WORKER-04
  - WORKER-06
  - WORKER-07
  - WORKER-08
  - SCHED-04
  - LIMIT-03
  - LIMIT-04
must_haves:
  truths:
    - "An end-to-end test publishes a scheduled post against a real Postgres + Redis using a mocked Twitter HTTP layer"
    - "An end-to-end test asserts retry exhaustion → failed state → notification event enqueued"
    - "An end-to-end test asserts SIGTERM gracefully drains an in-flight publish job"
    - "Idempotency under stalled-job recovery is asserted with a real BullMQ worker"
    - "Dimension 8 of 04-VALIDATION.md is filled in: every Phase 4 requirement maps to an automated test"
    - "All five Phase 4 success criteria are demonstrably TRUE via automated or manual verification"
  artifacts:
    - path: "packages/worker/src/__tests__/integration/post-lifecycle.integration.test.ts"
      provides: "End-to-end happy path + idempotency + retry + DLQ"
      min_lines: 80
    - path: "packages/worker/src/__tests__/integration/shutdown.integration.test.ts"
      provides: "Graceful shutdown integration test with real Worker against testcontainer Redis"
    - path: "packages/worker/src/__tests__/helpers/testcontainer.ts"
      provides: "Shared Postgres + Redis testcontainer factory for worker integration tests"
  key_links:
    - from: "post-lifecycle.integration.test.ts"
      to: "real BullMQ Worker + real Postgres + MSW-mocked Twitter"
      via: "testcontainer setup + createPublishWorker + msw setupServer"
      pattern: "createPublishWorker"
    - from: "04-VALIDATION.md Per-Task Verification Map"
      to: "every requirement ID for Phase 4"
      via: "filled-in test command per row"
      pattern: "WORKER-04|SCHED-04|LIMIT-04"
---

<objective>
Ship Phase 4 verification: integration tests against real Postgres + Redis (testcontainers) + MSW-mocked Twitter that exercise the full publish pipeline end-to-end, plus a graceful-shutdown integration test using a real BullMQ Worker. Update `04-VALIDATION.md` to fill in the Per-Task Verification Map for every Phase 4 requirement and flip `nyquist_compliant` to `true`. Conclude with a human checkpoint to confirm all five phase success criteria.

Purpose: Phase verification gate. Without this plan, Plans 01–05 are individually green but the seams between them (worker → DB → API → UI) are unverified.
Output: Three integration test files, a shared testcontainer helper, an updated validation document, and a human checkpoint signing off on phase completion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-publish-worker-scheduled-posts/04-CONTEXT.md
@.planning/phases/04-publish-worker-scheduled-posts/04-RESEARCH.md
@.planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md
@packages/worker/src/publish-worker.ts
@packages/worker/src/post-lifecycle.service.ts
@packages/worker/src/index.ts

<interfaces>
From Plans 01-05:
- Live `post_attempts` table + `social_profiles.monthly_tweet_budget` columns in dev Postgres
- `createPublishWorker`, `startScanner`, `createWorkerDb` from `@sms/worker`
- `createPublishQueueService` from `@sms/api/services/publish-queue.service`
- `seedPost`, `seedSocialProfile` test helpers from Plan 03 (or duplicated locally)
- `mock-twitter.ts` MSW handler factory from Plan 03
- All Phase 4 endpoints functional

testcontainers package (~10.x) available in packages/worker devDeps from Plan 02 task 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build testcontainer helper and run end-to-end publish lifecycle integration tests</name>
  <files>packages/worker/src/__tests__/helpers/testcontainer.ts, packages/worker/src/__tests__/integration/post-lifecycle.integration.test.ts, packages/worker/src/__tests__/integration/failed-listener.integration.test.ts, packages/worker/vitest.config.ts</files>
  <read_first>
    - packages/worker/src/publish-worker.ts (createPublishWorker signature; the failed listener payload shape)
    - packages/worker/src/post-lifecycle.service.ts (publishPost contract; PostLifecycleAbort reasons)
    - packages/worker/src/__tests__/helpers/seed-post.ts (Plan 03 seed helper — reuse for the integration tests)
    - packages/worker/src/__tests__/helpers/mock-twitter.ts (Plan 03 MSW handlers)
    - packages/worker/vitest.config.ts (current config — confirm test file globs include `**/__tests__/**/*.test.ts`)
    - .planning/phases/04-publish-worker-scheduled-posts/04-RESEARCH.md (Validation Architecture section — describes unit vs integration boundaries; "How to assert idempotency under race conditions" gives the test pattern)
    - .planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md (current state — what needs filling in)
  </read_first>
  <action>
    **1. `packages/worker/src/__tests__/helpers/testcontainer.ts`** — A shared helper that spins up a Postgres 17 testcontainer and a Redis 7.4 testcontainer for the integration tests. Pattern:

    ```typescript
    import { GenericContainer, type StartedTestContainer } from 'testcontainers';
    import { createWorkerDb, type WorkerDb } from '../../db.js';
    import IORedis, { type Redis } from 'ioredis';
    import { migrate } from 'drizzle-orm/postgres-js/migrator';

    export interface TestEnv {
      pgContainer: StartedTestContainer;
      redisContainer: StartedTestContainer;
      db: WorkerDb;
      redis: Redis;
      databaseUrl: string;
      redisUrl: string;
      stop: () => Promise<void>;
    }

    export async function startTestEnv(): Promise<TestEnv> {
      const pgContainer = await new GenericContainer('postgres:17-alpine')
        .withEnvironment({ POSTGRES_PASSWORD: 'test', POSTGRES_DB: 'sms_test' })
        .withExposedPorts(5432)
        .start();

      const redisContainer = await new GenericContainer('redis:7.4-alpine')
        .withExposedPorts(6379)
        .start();

      const databaseUrl = `postgres://postgres:test@${pgContainer.getHost()}:${pgContainer.getMappedPort(5432)}/sms_test`;
      const redisUrl = `redis://${redisContainer.getHost()}:${redisContainer.getMappedPort(6379)}`;

      const db = createWorkerDb(databaseUrl);
      // Apply Drizzle migrations
      await migrate(db, { migrationsFolder: 'packages/db/migrations' });

      const redis = new IORedis(redisUrl, { maxRetriesPerRequest: null });
      await redis.ping();

      const stop = async () => {
        try { await redis.quit(); } catch {}
        try { await pgContainer.stop(); } catch {}
        try { await redisContainer.stop(); } catch {}
      };

      return { pgContainer, redisContainer, db, redis, databaseUrl, redisUrl, stop };
    }
    ```

    Set `vitest.config.ts` `testTimeout` to at least 60000ms in `packages/worker/vitest.config.ts` for integration tests (testcontainer startup is slow). Use a separate `integration` test file pattern if needed.

    **2. `packages/worker/src/__tests__/integration/post-lifecycle.integration.test.ts`** — End-to-end happy path + edge cases:

    Tests:
    - Test 1 (happy path / WORKER-05): seed a `scheduled` post with `scheduledAt = now`, enqueue via real `publishQueueService.enqueuePublish`, run a real `createPublishWorker` with MSW intercepting the Twitter v2 tweet endpoint to return success, await up to 10s, assert post in DB has `status='published'`, `platformPostId='tweet-123'`, `publishedAt` not null, AND a `post_attempts` row exists with `outcome='success'`.
    - Test 2 (idempotency / WORKER-06): seed a post with `platformPostId='already-tweeted'` already set; enqueue and run worker; assert Twitter MSW handler was NEVER called and the worker logged the idempotent skip.
    - Test 3 (transient retry / WORKER-04): MSW returns 503 on attempts 1 and 2, 200 on attempt 3. Use `vi.useFakeTimers()` carefully OR shorten the backoff for the test by injecting an alternative backoff strategy. Assert post ends up `published` and `post_attempts` has 3 rows (2 transient_fail + 1 success).
    - Test 4 (permanent failure / SCHED-04 / D-10): MSW returns 401 (auth revoked). Assert post ends up `failed`, `failureReason` mentions auth revoked, only ONE `post_attempts` row with `outcome='permanent_fail'`, and BullMQ failed-listener fired exactly once.
    - Test 5 (rate limit runtime check / LIMIT-03): seed `monthly_tweet_budget = 1` and seed 1 already-published post for the profile this month. Enqueue a new scheduled post. Run worker. Assert the new post stays in `scheduled` status (worker re-checks budget and aborts) AND no Twitter HTTP call was made.

    Use the MSW `setupServer` pattern:
    ```typescript
    import { setupServer } from 'msw/node';
    import { http, HttpResponse } from 'msw';
    const server = setupServer();
    beforeAll(() => server.listen({ onUnhandledRequest: 'error' }));
    afterEach(() => server.resetHandlers());
    afterAll(() => server.close());
    ```

    **3. `packages/worker/src/__tests__/integration/failed-listener.integration.test.ts`**:
    - Test 1 (WORKER-07 / D-11): exhaust retries (4 attempts of MSW returning 500), assert a `notification` queue job exists with kind `publish_failed`, postId matching, and reason populated. Use the testcontainer Redis and create the notification queue inline.

    Each test cleans up DB rows in `afterEach` and stops the testcontainers in `afterAll`. Use a single `beforeAll` to start the testcontainers (sharing across tests in one file is acceptable since each test seeds its own posts).

    Note on test runtime: testcontainer startup is ~5-10s per container, so each `beforeAll` adds ~15s. Keep all integration tests grouped per file to amortize.

    **4. `packages/worker/vitest.config.ts`** — Update to:
    - Increase `testTimeout` to 60000 for integration tests
    - Optionally separate integration tests into their own project so they can be skipped in fast-loop dev runs. If splitting is too invasive for this task, just bump the timeout globally.
  </action>
  <verify>
    <automated>pnpm --filter @sms/worker vitest run src/__tests__/integration/post-lifecycle.integration.test.ts src/__tests__/integration/failed-listener.integration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/worker/src/__tests__/helpers/testcontainer.ts` exists and exports `startTestEnv`
    - `packages/worker/src/__tests__/integration/post-lifecycle.integration.test.ts` exists with at least 5 `it(` blocks
    - `packages/worker/src/__tests__/integration/failed-listener.integration.test.ts` exists with at least 1 `it(` block
    - grep finds `setupServer` and MSW handler usage in `post-lifecycle.integration.test.ts`
    - grep finds `await migrate(` in testcontainer.ts (Drizzle migrations applied to test DB)
    - grep finds `maxRetriesPerRequest: null` in testcontainer.ts
    - grep finds `kind: 'publish_failed'` or matching string in `failed-listener.integration.test.ts`
    - `pnpm --filter @sms/worker vitest run src/__tests__/integration/` exits 0
    - All integration tests verified to actually start containers (not silently skipped — the test runtime should be > 30s)
  </acceptance_criteria>
  <done>End-to-end integration tests pass against real Postgres + Redis with mocked Twitter, exercising happy path, idempotency, retry, permanent failure, runtime budget abort, and DLQ notification.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build graceful shutdown integration test and finalize 04-VALIDATION.md</name>
  <files>packages/worker/src/__tests__/integration/shutdown.integration.test.ts, .planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md</files>
  <read_first>
    - packages/worker/src/index.ts (main() shutdown logic — closeWithTimeout pattern)
    - .planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md (CURRENT — has a placeholder row in the Per-Task Verification Map)
    - .planning/REQUIREMENTS.md (Phase 4 requirement IDs to enumerate)
    - packages/worker/src/__tests__/helpers/testcontainer.ts (created in Task 1)
  </read_first>
  <action>
    **1. `packages/worker/src/__tests__/integration/shutdown.integration.test.ts`** (WORKER-08):

    Test plan:
    - Test 1: Spin up testcontainer Postgres + Redis. Construct a real BullMQ Worker via `createPublishWorker`. Add a job whose handler intentionally takes ~3 seconds (mock the publishPost dep with a delay). After the job is `active`, call `worker.close()` with the `closeWithTimeout` pattern from `main()`. Assert close resolves within the 30s window AND the in-flight job either completes or is gracefully released. Confirm no orphan jobs in the active set after close.
    - Test 2: Same setup but the handler takes 35 seconds. Call close with the 30s timeout. Assert the timeout fires and the test reports the failed cleanup step (per CLAUDE.md shutdown rule: one failure must not skip the rest — the redis.quit() should still happen). Use `vi.spyOn` on the logger to assert a `Shutdown step failed` line was emitted.

    **2. Update `.planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md`:**

    Replace the placeholder Per-Task Verification Map with the full Phase 4 requirements. For each requirement ID, populate:
    - Plan number (1-6)
    - Wave (1-5)
    - Test type (unit / integration / manual)
    - Automated command (the exact `pnpm` invocation that exercises the test)
    - File exists status (✅ for completed plans)
    - Status (✅ green / ⚠️ flaky / ❌ red — set based on actual test run)

    Example rows (build the full table):

    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 4-01-01 | 01 | 1 | LIMIT-01 | T-04-01-04 | Budget column exists with default | unit | `psql -c "\d social_profiles" \| grep monthly_tweet_budget` | ✅ | ✅ |
    | 4-01-02 | 01 | 1 | SCHED-04 | T-04-01-01 | post_attempts table exists with cascade FK | unit | `psql -c "\d post_attempts"` | ✅ | ✅ |
    | 4-02-01 | 02 | 2 | WORKER-04 | T-04-02-* | classifier categorizes errors correctly | unit | `pnpm -F @sms/api vitest run src/services/__tests__/rate-limit.service.test.ts` | ✅ | ✅ |
    | 4-02-02 | 02 | 2 | LIMIT-04 | T-04-02-03 | budget bounds validated | unit | `pnpm -F @sms/api vitest run src/services/__tests__/rate-limit.service.test.ts` | ✅ | ✅ |
    | 4-03-01 | 03 | 3 | WORKER-04 | — | backoff schedule + retry-after | unit | `pnpm -F @sms/worker vitest run src/__tests__/backoff.test.ts` | ✅ | ✅ |
    | 4-03-02 | 03 | 3 | WORKER-06 | T-04-03-03 | idempotency via platform_post_id | integration | `pnpm -F @sms/worker vitest run src/__tests__/post-lifecycle.test.ts` | ✅ | ✅ |
    | 4-03-03 | 03 | 3 | LIMIT-03 | T-04-03-04 | runtime budget re-check aborts publish | integration | `pnpm -F @sms/worker vitest run src/__tests__/post-lifecycle.test.ts` | ✅ | ✅ |
    | 4-04-01 | 04 | 3 | SCHED-03 | T-04-04-01 | Retry endpoint enforces ownership + state | integration | `pnpm -F @sms/api vitest run src/__tests__/routes/retry.test.ts` | ✅ | ✅ |
    | 4-04-02 | 04 | 3 | SCHED-04 | T-04-04-02 | History endpoint groups by cycle | integration | `pnpm -F @sms/api vitest run src/__tests__/routes/history.test.ts` | ✅ | ✅ |
    | 4-04-03 | 04 | 3 | LIMIT-01/02 | T-04-04-03/04 | Rate limit GET/PATCH with strict Zod | integration | `pnpm -F @sms/api vitest run src/__tests__/routes/rate-limit.test.ts` | ✅ | ✅ |
    | 4-04-04 | 04 | 3 | WORKER-07 | T-04-04-06 | Bull-Board behind requireAuth | integration | `pnpm -F @sms/api vitest run src/__tests__/routes/admin.test.ts` | ✅ | ✅ |
    | 4-05-01 | 05 | 4 | SCHED-01/02 | T-04-05-01 | Posts page shows error column + polling | component | `pnpm -F @sms/web vitest run src/__tests__/components/PostHistoryDialog.test.tsx` | ✅ | ✅ |
    | 4-05-02 | 05 | 4 | LIMIT-04 | T-04-05-03 | Rate limit settings dialog with validation | component | `pnpm -F @sms/web vitest run src/__tests__/components/RateLimitSettingsDialog.test.tsx` | ✅ | ✅ |
    | 4-06-01 | 06 | 5 | WORKER-04/05/06/07 | T-04-03-03 | E2E publish + retry + DLQ | integration | `pnpm -F @sms/worker vitest run src/__tests__/integration/post-lifecycle.integration.test.ts` | ✅ | ✅ |
    | 4-06-02 | 06 | 5 | WORKER-08 | T-04-03-08 | Graceful shutdown drains in-flight | integration | `pnpm -F @sms/worker vitest run src/__tests__/integration/shutdown.integration.test.ts` | ✅ | ✅ |
    | 4-06-03 | 06 | 5 | WORKER-07 | — | DLQ notification event emitted | integration | `pnpm -F @sms/worker vitest run src/__tests__/integration/failed-listener.integration.test.ts` | ✅ | ✅ |

    Add a manual row for SCHED-01 polling visual + Bull-Board click-through, marked as `manual`, with the verification instructions referencing Plan 05 Task 3 checkpoint.

    Update the frontmatter:
    - `nyquist_compliant: true` (after confirming all listed tests are green)
    - `wave_0_complete: true`

    Update Validation Sign-Off checklist — check all five boxes:
    - [x] All tasks have automated verify or Wave 0 dependencies
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify
    - [x] Wave 0 covers all MISSING references
    - [x] No watch-mode flags
    - [x] Feedback latency < 90s (note: integration tests with testcontainers exceed 90s — document this exception in the Manual-Only Verifications section)

    Set `Approval: APPROVED` and add the date.
  </action>
  <verify>
    <automated>pnpm --filter @sms/worker vitest run src/__tests__/integration/shutdown.integration.test.ts && node -e "const fs=require('fs'); const v=fs.readFileSync('.planning/phases/04-publish-worker-scheduled-posts/04-VALIDATION.md','utf8'); if (!v.includes('nyquist_compliant: true')) { console.error('nyquist_compliant not set true'); process.exit(1); } const reqs=['WORKER-01','WORKER-04','WORKER-06','WORKER-07','WORKER-08','SCHED-01','SCHED-03','SCHED-04','LIMIT-01','LIMIT-04']; for (const r of reqs) { if (!v.includes(r)) { console.error('missing requirement in validation table:', r); process.exit(1); } } console.log('ok');"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/worker/src/__tests__/integration/shutdown.integration.test.ts` exists with ≥ 2 tests
    - `pnpm --filter @sms/worker vitest run src/__tests__/integration/shutdown.integration.test.ts` exits 0
    - `04-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`
    - `04-VALIDATION.md` Per-Task Verification Map contains rows for every Phase 4 requirement ID (WORKER-01 through WORKER-08, SCHED-01 through SCHED-04, LIMIT-01 through LIMIT-05) — at least 17 rows
    - Each row has a populated `Automated Command` column (no empty cells)
    - Validation Sign-Off checklist all 5 boxes checked
    - `Approval: APPROVED` set in the document
  </acceptance_criteria>
  <done>Graceful shutdown integration test passes; 04-VALIDATION.md is fully populated and signed off; nyquist_compliant flag flipped to true.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 4 final sign-off — five success criteria check</name>
  <what-built>
    All six plans for Phase 4 are complete:
    - Plan 01: Schema additions (post_attempts table, social_profiles columns) + migration applied
    - Plan 02: Shared queue constants, error classifier, rate limit service, publish queue factory, dependencies installed
    - Plan 03: Publish worker, scanner, lifecycle service, twitter publish service, graceful shutdown
    - Plan 04: Retry, history, rate-limit, admin endpoints with security checks
    - Plan 05: Full UI for posts list extensions, history modal, rate limit banner/block/settings
    - Plan 06: End-to-end integration tests + validation document finalized
  </what-built>
  <how-to-verify>
    Verify each of the five Phase 4 success criteria from ROADMAP.md:

    **Success Criterion 1:** *Scheduled Twitter posts publish automatically at their scheduled time without user intervention; published posts show `platform_post_id` and `published_at` timestamp.*
    1. Run the worker against dev Postgres + Redis: `pnpm dev` or the project's standard dev command
    2. Use the web app to create a new Twitter post scheduled for `now + 1 minute` against a real Twitter test profile (or use the MSW dev mode if real Twitter not desired)
    3. Wait up to 90 seconds. Confirm:
       - The post in `/posts` transitions from `scheduled` → `publishing` → `published` (visible via the polling refresh)
       - `psql -c "SELECT id, status, platform_post_id, published_at FROM posts WHERE id = '...'"` returns non-null `platform_post_id` and `published_at`

    **Success Criterion 2:** *Failed publishes retry with exponential backoff (max 3 retries); exhausted retries move the post to `failed` state and land in the dead letter queue.*
    1. Temporarily revoke the Twitter test profile's tokens (or use MSW dev mode to return 500 for the next 4 attempts)
    2. Schedule a post; observe in Bull-Board (`/admin/queues`) that the job retries with the 30s → 5min → 30min schedule
    3. After all 4 attempts, confirm:
       - Post status = `failed` in the DB
       - `failureReason` is populated
       - The job is in BullMQ's failed set (visible in Bull-Board)
       - The Posts page shows the new error column with the truncated reason

    **Success Criterion 3:** *Stalled job recovery does not cause duplicate posts — worker checks `platform_post_id` before re-attempting publish.*
    1. Verified by integration test `post-lifecycle.integration.test.ts` Test 2 (idempotency)
    2. Manual confirmation: psql update to set a post's `platform_post_id` to a sentinel value before the worker picks it up; observe the worker logs `Idempotent: post already has platform_post_id — skipping`

    **Success Criterion 4:** *User can view all scheduled posts in a filterable list with per-post actions (edit, delete, view history, view full text).*
    1. Already verified in Plan 05 Task 3 human checkpoint
    2. Re-confirm each kebab action visible and functional, polling indicator updating

    **Success Criterion 5:** *Twitter rate limit tracking respects the user's configured monthly budget; publishing is blocked when budget is reached; new posts show pre-flight warning at 90%.*
    1. Already verified in Plan 05 Task 3 human checkpoint (warn banner + block error + settings dialog)
    2. Worker-side check: lower budget below current count, then enqueue a job manually via `pnpm -F @sms/api node -e "..."` or by API call, observe worker logs `budget_exhausted` skip

    **Final regression check:**
    - `pnpm -w vitest run` exits 0 (entire monorepo)
    - `pnpm -w tsc --noEmit` exits 0
    - No regressions in Phase 1-3 tests
    - Bull-Board accessible at `/admin/queues` only when authenticated

    **Documentation check:**
    - 04-SUMMARY.md exists and references all six plan summaries
    - ROADMAP.md Phase 4 plans list updated to show all 6 plans completed (or in progress)
  </how-to-verify>
  <resume-signal>Type "approved" if all five success criteria are demonstrably TRUE and the regression check passes. Otherwise describe each failing criterion with evidence (psql output, screenshot reference, or test failure log).</resume-signal>
  <files>(human verification — no files modified)</files>
  <action>HUMAN CHECKPOINT — see <how-to-verify> above. Executor must NOT skip this step. Pause execution and present the five-criteria verification steps to the user verbatim. Do not mark Phase 4 complete until the user types "approved".</action>
  <verify>Manual verification per <how-to-verify> steps. No automated command — this is the phase-gate human checkpoint by design.</verify>
  <done>User has confirmed all five Phase 4 success criteria are TRUE and the full monorepo regression suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test environment → testcontainer Postgres/Redis | Isolated containers; credentials are test-only |
| MSW interception → twitter-api-v2 client | Test-only; never reaches real Twitter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Information Disclosure | Test fixtures could leak real Twitter credentials if misused | mitigate | Tests use seeded fake credentials only. The MSW mock handlers never forward to real Twitter. The testcontainer Redis/Postgres are ephemeral and stopped after each test. |
| T-04-06-02 | Denial of Service | Slow integration tests block CI | accept | Documented in 04-VALIDATION.md "Manual-Only Verifications" / "Sampling Rate" sections — integration tests run on phase-gate, not on every commit. Unit tests run on every commit. |
| T-04-06-03 | Tampering | Migration applied to test DB diverges from prod | mitigate | testcontainer.ts uses the same `packages/db/migrations/` folder via `migrate()` — no separate test schema. Plan 01's migration is the single source of truth. |
| T-04-06-04 | Information Disclosure | Test logs could leak fake credentials, future grep noise | accept | Test fixtures use obviously-fake values like `'fake-access-token'` — not confusable with real credentials. |
</threat_model>

<verification>
1. `pnpm --filter @sms/worker vitest run src/__tests__/integration/` exits 0 (all integration tests pass)
2. `pnpm -w vitest run` exits 0 (no regressions across packages)
3. `pnpm -w tsc --noEmit` exits 0
4. `04-VALIDATION.md` frontmatter `nyquist_compliant: true`
5. Human checkpoint passes for all 5 success criteria
</verification>

<success_criteria>
- All five Phase 4 success criteria verified TRUE (3 via integration tests, 2 via human checkpoint built in earlier plans)
- testcontainer-based integration tests pass: post lifecycle (5 tests), shutdown (2 tests), failed listener (1 test)
- 04-VALIDATION.md is the canonical map of every Phase 4 requirement → automated command, with `nyquist_compliant: true`
- No regressions in Phase 1-3 unit tests
- Phase 4 ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/04-publish-worker-scheduled-posts/04-06-SUMMARY.md` documenting: integration test counts, total Phase 4 test count (sum across all plans), validation map status, any deviations from RESEARCH.md verification architecture, and the date Phase 4 was signed off.

Then create `.planning/phases/04-publish-worker-scheduled-posts/04-SUMMARY.md` (the phase-level summary) referencing all six plan summaries and recording:
- Final list of new files
- Final list of new dependencies
- All requirement IDs covered (with cross-references to plan files)
- Threat model summary across all plans
- Manual verification outcomes
- Carryover items (e.g., thread publishing deferred — see Plan 03 Task 1 PostLifecycleAbort `thread_unsupported`)
- Phase 5 prerequisites that this phase produced
</output>
